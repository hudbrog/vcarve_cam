# Revision 2 screenshot prompts

Built-in `image_gen` generation/editing. The original `../01-setup.png` is a visual-style reference only, not an existing application screenshot. Each exact screen prompt below is appended to the shared prompt. Output files are new revision assets; earlier references remain available.

## Shared prompt

Use case: ui-mockup. Produce one high-fidelity fake screenshot of a revised 2.5D CAM application for experienced CNC users. Use the supplied original generated Setup screenshot as a visual-style reference only: preserve its graphite header, pale gray flat panels, crisp compact English type, small rectangular controls, teal selection, orange active mode and ordinary egui-feasible look. The new screen instructions REPLACE the old navigator and content. Wide landscape application-only screenshot, around 1920 x 1080 composition, no browser or desktop chrome. Header: "2.5D CAM", document "Panel study", Prepare active, Simulate, Generate, Export... . Calm dense engineering UI with thin separators and minimal padding, not marketing cards. No claims of safety or implemented capabilities. All example values are synthetic. Status bar includes "Concept / illustrative values".
The left navigator remains visible in ALL screens and has FOUR groups. SETUP: "Stock & work zero", "Machine  Workshop", "Tool library  Global". ARTWORK: "outline.svg", "lettering.svg", "+ Import artwork". OPERATIONS: "01 Face / T1", "02 Flat V-carve / T1 > T2", "03 Profile / T1", "+ Operation". At the BOTTOM, "JOB TOOLS (2)", "T1  6 mm endmill", "T2  60 deg V-bit", "Manage...". Job Tools must be visually separate from the global Tool library link. Show no Pocket, no rotary axis, no sketch creation button, no device controls. Do not add other operations. Left pane may be about 280 px and use compact 28 px rows to fit all groups.

## 01 — Multiple artwork sources

Select the "lettering.svg" artwork row in the left navigator. Both artwork rows have visible eye icons and closed/open padlock controls. Center is a large top orthographic XY viewport of a tan rectangular stock sheet with a gray rounded-rectangle plaque outline and two circular holes belonging to outline.svg. Inside the plaque, thin outlined vector letters spell "NORTH" in teal and belong to lettering.svg; this represents letters already converted to SVG paths, not a text-editing feature. Only the lettering has a teal selection bounding box with corner handles. Two small leader labels identify "outline.svg" and "lettering.svg". No generated toolpaths or cut grooves. Viewport small text "Artwork placement / 2 sources". Right inspector heading "lettering.svg" and scope badge "Artwork item". SOURCE group: "Type  SVG", "Import  Filled regions", button "Replace source...". PLACEMENT group: "X  40.00 mm", "Y  30.00 mm", "Rotation  0 deg", "Scale  100 %", button "Center on stock". USAGE group "Used by 02 Flat V-carve" and a link "Show assigned geometry". Footer note "Placement applies to this source only." Navigator Machine shows applied label Workshop, Tool library shows Global, and bottom job tools remain visible. Status "2 artwork items / 2 job tools / No toolpaths generated". The actual source dimensions are illustrative; do not add a common global artwork transform or automatically selected third file.

## 02 — Global library: one geometry, several cutting profiles

Select "Tool library Global" under SETUP. The main area temporarily replaces the stock viewport and ordinary inspector with a wide tool-library editor; KEEP the whole left job navigator and bottom job-tools group visible. Main heading "Tool library" with scope badge "Global" and short note "Reusable tools and cutting profiles". A searchable narrow tool list within the main area contains selected "6 mm endmill", "60 deg V-bit", and "Drag knife". Above this list modest buttons "+ Tool" and "Import / Export". The selected tool's workspace has a small generic endmill geometry illustration, heading "6 mm endmill", and compact GEOMETRY fields "Diameter 6.00 mm", "Cutting length 18.00 mm", "Flutes 2", checkbox "Plunge capable". Geometry appears ONCE for the selected tool, not once per cutting profile. Next to/below it a clearly labeled "Cutting profiles (3)" table with columns "Name", "Material", "Machine": selected "MDF - face", "MDF", "Workshop"; "MDF - profile", "MDF", "Workshop"; "Plywood - finish", "Plywood", "Workshop". Buttons "+ Profile", "Duplicate", "Save profile". Detail area for selected "MDF - face": "Spindle 18000 rpm", "Cutting feed 1200 mm/min", "Plunge feed 300 mm/min", "Stepdown 0.50 mm", "Stepover 3.00 mm". Mark these as "Illustrative profile values". Clear bottom actions "Add geometry to job" and "Use in operation...". Small footer "Library edits do not update saved jobs." Material/machine are context labels, not automatic configuration selection. Do not show T/H machine numbers as global geometry fields. Preserve selected job tools at the bottom of the outer navigator.

## 03 — Job tools and operation-specific cutting values

Select bottom "JOB TOOLS (2) / Manage...". Main area replaces the viewport with "Job tools" and scope badge "This job". KEEP SETUP with Machine and Tool library Global, the two ARTWORK files, and the ordered OPERATIONS list in the left navigator. Main top toolbar "Add from library", "Create job tool", "Remove unused". Compact tool table columns "Tool", "Geometry", "Used by", "Controller T/H". Selected row "T1 / 6 mm endmill", "6.00 mm / 18.00 mm reach", "Face, Carve rough, Profile", "T1 / H1". Second row "T2 / 60 deg V-bit", "60 deg", "Carve V-bit", "T2 / H2". Link "Edit mapping in Setup > Machine" below table; mappings are a readout, not a second editable owner. Lower main pane heading "T1 assignments" with table columns "Operation / stage", "Cutting profile", "Feed", "Spindle", "Status". Three rows: "01 Face", "MDF - face", "1200 mm/min", "18000 rpm", "Applied"; "02 Carve / Rough", "Custom", "900 mm/min", "18000 rpm", "Custom"; "03 Profile", "MDF - profile", "1000 mm/min", "18000 rpm", "Modified". Select the Profile row. A compact detail pane to the right shows "03 Profile / T1", "Cutting profile MDF - profile", badge "Modified", "Library value 900 mm/min", "Operation value 1000 mm/min", buttons "Open operation", "Reapply profile", "Save as profile...". Small summary "One tool geometry / Three assignments" and footer "Changing one assignment leaves the others unchanged." Geometry-edit action separately labeled "Edit job geometry..." with note "Used by 3 assignments". Do not show a single globally active cutting profile for T1. No recomputed toolpaths/check-pass badges; this is an editing/manage view. Values are synthetic.
