# 2.5D CAM implementation checklist

Companion progress tracker for the plan in [2.5d-cam-plan.md](2.5d-cam-plan.md).
Slices, dependencies, boundaries and acceptance criteria come from plan section 18
and the [UI-driven backend addendum](2.5d-cam-plan.md#22-addendum-ui-driven-backend-contracts-2026-09-10);
fixture matrices from sections 19 and 22.12; commands from section 19.3.

Scope update (2026-09-10): F3 is now a **Rust-only** knife integration slice, specified
in [section 22.10](2.5d-cam-plan.md#2210-f3-revised-rust-knife-integration-and-output-evidence).
The A1–F2 records below remain historical implementation evidence. Their references
to future F3 UI controls/playback are superseded by the [new UI plan](2.5d-cam-ui-plan.md);
they are not part of revised F3 acceptance. H1–H6 track the additional backend work
needed by that UI. This documentation update does not complete any implementation slice.

Status legend: ☐ not started · ◐ in progress · ✅ done (acceptance evidence recorded) · ⏸ blocked

| Slice | Depends on | Status | Summary |
| --- | --- | --- | --- |
| A1 canonical model | — | ✅ | `project::CamJob` schema 4, operation enums, setup, assignments, migration |
| A2 sequence contract | A1 | ✅ | Generic motions/stages/execution + Flat V-carve adapter |
| A3 core checks/post split | A2 | ✅ | Basic checks split from M5; generic ordered writer/reader; profile migration; retained-plan trust |
| A4 both adapters | A3 | ✅ | `ui-8` DTOs, native/WASM dispatch, operation-list editing/recovery |
| B1 physical stock | A4 | ✅ | Stock dimensions/placement, origin presets, output transforms, real rectangle in previews |
| B2 stock prefixes/heights | B1 | ✅ | History queries, ordered dependencies, concrete height references |
| C1 face core | B2 | ✅ | Rectangle raster 0/90°, depth layers, extensions, entry logic, coverage checks |
| C2 face workflow | C1 | ✅ | UI area/margin controls, face outputs, uniform-top V-carve adapter |
| D1 contour catalogue | B1 | ✅ | Stable closed-contour IDs, holes, explicit side, anchors |
| D2 basic profile | D1, B2 | ✅ | Offsets, cutting direction, depth passes, through allowance, plunge entry |
| D3 profile workflow | D2, C2 | ✅ | UI profile editing, combined sequence export, stock/timeline display |
| E1 tabs | D3 | ✅ | Protected tab footprints, rectangular tabs, manual/automatic placement |
| E2 finishing | E1 | ✅ | Radial allowance and depth-stepped final contour with separate feed |
| E3 entries/inspection | E2 | ✅ | Contour ramps, straight/arc leads, start editing, tab/depth inspection |
| F1 knife data/import | E3 | ✅ | Knife tool/presets, open-chain importer, spindle-off stage prep, pivot/tip display contract |
| F2 knife geometry | F1 | ✅ | Alignment, compensation, corner swivels, depth/closure, independent replay |
| F3 knife Rust integration | F2 | ✅ | Actual-byte modal checks/replay, prefix contact, bounded evidence and typed Rust APIs; no UI implementation |
| G later operations | F3 | ☐ | Out of scope until separately planned |
| H1 portable document/drafts | A1–F2 | ✅ | Schema-5 artwork/ref/snapshot/provenance shapes, migration and saveable unresolved references |
| H2 artwork/catalogue commands | H1 | ✅ | Document-level resolver/combined owner-qualified catalogue, artwork/assignment/fit-stock commands |
| H3 multi-source machining | H2 | ✅ | Cross-source selections, resolved-region Flat V-carve input and semantic identities |
| H4 resources/inspection | H3; F3 for knife evidence | ✅ | Applied cutting/machine snapshots, reset/reapply, new transport and inspection DTOs |
| H5 retained execution/output | H3, H4 | ☐ | Cancellable tasks, motion paging, scoped output bundles and exact-byte retry |
| H6 backend qualification | H1–H5, F3 | ☐ | Native/CLI/WASM parity, limits, recovery and evidence handoff; excludes UI completion |

Rules from the plan (sections 18 and 22.11): follow the declared dependencies and finish
a prerequisite's acceptance criteria before its dependent slice; report changed files,
behavior, test evidence and remaining unsupported cases per slice; update statuses only
after evidence exists. Software fixture success does not imply a physical machining trial.

F3 can be completed against schema 4 before H1. General pocketing and other G operations
do not gate H1–H6. The egui feasibility spike and UI milestones remain in the UI plan.

---

## A1: canonical model — ✅ done (2026-09-09)

Boundary: add `project::CamJob` (schema 4) with operation enums, setup, tool model and
assignments, plus strict migration from legacy schema-3 jobs. Legacy algorithms unchanged.

Acceptance criteria (from plan section 18):

- [x] Old complete jobs round-trip through migration with all values and selection preserved.
      (`project_migration.rs`: schema-2 endmill-only `fixtures/m3/rectangle.json`, combined
      `fixtures/m4/contact-line.json`, and the real combined flower job
      `real_data/flower_box-svg.job-real.json` — value-level assertions per field plus a
      schema-4 JSON serialize/reload identity check.)
- [x] Old incomplete jobs round-trip with missing fields preserved as unset.
      (`incomplete_legacy_job_migrates_with_unset_values_preserved`: cleared planning block,
      stock thickness, depth and per-tool values stay `None`; endmill-only mode preserved.)
- [x] A Face operation can exist in a job with no SVG source.
      (`face_job_without_source_validates_and_round_trips`; incomplete face remains saveable.)
- [x] Duplicate operation/tool IDs are rejected. (`PROJECT_OPERATION_ID` / `PROJECT_TOOL_ID`.)
- [x] Unresolved tool/height references are rejected with located errors.
      (`PROJECT_TOOL_REFERENCE`, `PROJECT_HEIGHT_REFERENCE`, `HEIGHT_REFERENCE_FORWARD`,
      `HEIGHT_REFERENCE_INVALID`, `PROJECT_TOOL_KIND` — messages carry operation/tool IDs.)
- [x] Unknown job schemas are rejected. (`CAM_JOB_SCHEMA_VERSION` for versions 3/5;
      unknown operation kinds and unknown fields fail strict parsing as `PROJECT_JSON`.)
- [x] `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`,
      `cargo test --workspace --locked` pass (37 test targets ok, 0 failures, including the
      pre-existing suites).

Changed files: `crates/cam-core/src/project.rs` (new), `crates/cam-core/src/project/migrate.rs`
(new), `crates/cam-core/src/lib.rs` (module wiring), `crates/cam-core/tests/project_model.rs`
(new, 16 tests), `crates/cam-core/tests/project_migration.rs` (new, 7 tests). Additive-only
touches to legacy files for shared derives: `job.rs`, `model.rs`, `pocket/settings.rs`,
`vcarve/settings.rs` (`PartialEq` derives; no behavior change).

Behavior notes:

- `CamJob` schema 4 with `FlatVcarve | Face | Profile | DragKnife` settings, `SetupSettings`
  (stock thickness/XY, work zero incl. `StockAnchor` fractions 0/0.5/1, clearance, start),
  `JobTool` with pure geometry + one authoritative plunge capability in `ToolCapabilities`,
  `MillingAssignment`/`KnifeAssignment` with `spindle_direction` kept unset on migration.
- Migration maps legacy `endmill_planning.clearance_z_mm`/`start_xy_mm` to setup travel
  settings, rough budgets to the operation, `vbit_planning` presence to
  `Combined`/`EndmillOnly` mode, conflicting legacy plunge values rejected by the legacy
  validator before migration, machine profile preserved as `legacy_machine_profile`.
- Validation allows incomplete jobs to save (Options stay unset); structural errors
  (duplicate IDs, unknown references, forward height refs, misplaced `OperationTop`,
  mode/finish disagreement) are document errors even in disabled operations.

Remaining unsupported cases: CamJob is not yet consumed by planning, service DTOs, adapters
or UI (that is A2–A4); height references are validated structurally only (numeric
resolution arrives with B2); Face/Profile/DragKnife settings are document-contract fields
only — no planner exists yet; stock XY stays `None` for legacy jobs until supplied (B1).

## A2: sequence contract — ✅ done (2026-09-09)

Boundary: generic motions/stages/execution representation plus the Flat V-carve
compatibility adapter, planned against original stock-top geometry.

Acceptance criteria (from plan section 18):

- [x] Migrated flower and synthetic M4 jobs produce equivalent legacy motion
      geometry/order and completeness.
      (`sequence_plan.rs::migrated_m4_and_flower_plans_match_legacy_geometry_order_and_completeness`
      over `fixtures/m3/rectangle.json` (endmill-only), `fixtures/m4/contact-line.json`
      (combined with an empty rough stage) and the real combined flower job:
      per-motion start/end/layer/feed equality in order, interpolation/effect equal to
      the old writer's actual G0/G1 and cutting semantics, stage ranges exact,
      execution items `ToolChange → SetProcessIntent → RunStage` in order.
      `adapter_reconstructs_the_migrated_legacy_job` proves the adapted legacy job is
      value-identical to the migrated source, modulo the redundant endmill slot
      plunge spelling.)
- [x] Two independent operations have distinct identities.
      (`two_independent_operations_keep_distinct_identities`: unique stage IDs
      namespaced by operation, dense global motion IDs with one contiguous range per
      operation, recurring tools re-selected in execution order without regrouping
      motions by tool, input/execution fingerprints differ from the single-op plan.)
- [x] Extra contract behaviors: missing machining fields yield an `incomplete`
      operation result with located diagnostics (never a silent skip); unsupported
      enabled operations fail with `OPERATION_PLANNER_UNAVAILABLE`; budget-exhausted
      fixtures report `inconclusive`, not success; every linear-feed motion carries a
      feed; V-bit pass IDs derive from recorded legacy executions; unresolved spindle
      direction is recorded as an export `preparation_requirement`, not executable
      state. (`sequence_plan.rs`, 7 tests.)

Changed files: `crates/cam-core/src/toolpath.rs` (new), `crates/cam-core/src/sequence.rs`
(new), `crates/cam-core/src/operations/mod.rs` (new), `crates/cam-core/src/operations/flat_vcarve.rs`
(new), `crates/cam-core/src/lib.rs` (module wiring), `crates/cam-core/tests/sequence_plan.rs`
(new, 7 tests).

Test evidence: `cargo test -p cam-core --locked --test sequence_plan` → 7 passed;
`cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --locked -- -D warnings`,
`cargo test --workspace --locked` all pass (0 failures).

Remaining unsupported cases: plans use original stock-top geometry (prefix stock
history arrives in B2); `OperationPlan` is produced in cam-core only — it is not yet
served through cam-service DTOs, native/WASM adapters or the web UI (A4), cannot be
exported (A3 writer/reader), and Face/Profile/DragKnife operations are rejected rather
than planned (C/D/F). Empty legacy rough stages are skipped rather than emitted as
empty stages.

## A3: core checks/post split — ✅ done (2026-09-09)

Boundary: automatic basic checks separated from M5 detailed quality; generic ordered
LinuxCNC writer/reader for sequence plans; schema-1 profile application; trusted
retained-plan handling for imported plans.

Acceptance criteria (from plan section 18):

- [x] New sequence export works without running detailed quality.
      (`sequence_export.rs::sequence_export_works_without_running_detailed_quality` and
      `combined_fixture_exports_through_the_sequence_pipeline`: `check_plan` →
      `PreparedExecution::prepare` → emit → independent readback; M5/`verify_motions` is
      never invoked on this path. `legacy_quality_analysis_remains_callable_alongside_sequence_export`
      proves the legacy planner/analysis still run.)
- [x] Altered motion order/process state are rejected.
      (`altered_motion_order_or_coordinates_are_rejected_by_readback`: swapped motion
      blocks, edited coordinates, changed tool numbers and removed spindle starts are
      rejected with `POST_SEQUENCE_MISMATCH` / `PROCESS_SPINDLE_STATE` by a reader that
      trusts only the numeric bytes.)
- [x] Forged imported receipts are rejected.
      (`forged_imported_plans_and_receipts_are_rejected`: `TrustedPlan::import` replans
      the embedded job and compares motions/stages/execution/fingerprints — edited
      motions, recomputed-looking fingerprints, dropped motions and foreign engine
      versions are all rejected; only regeneration can establish trust.)
- [x] Behavior details: basic checks (`checks.rs`) cover generation completeness, dense
      motion IDs, stage ownership/order/coverage, finite coordinates, positive feeds on
      linear feeds, no feeds on rapids, effect/role agreement and full execution
      accounting (tool change → intent → run per stage, nothing extra). Unresolved
      spindle direction blocks process preparation (`PROCESS_SPINDLE_STATE`) but not
      generation, and is recorded as a preparation requirement. Schema-2 sequence
      profiles map any number of job tools exactly once, carry no `z_datum` and no
      per-tool spindle direction (strict parsing rejects both). Applying the real
      schema-1 profile moves its Z datum into `setup.work_zero.z` and its per-tool
      directions into matching assignments as one application. Stock-bottom Z-datum
      export shifts Z by stock thickness (machine Z13.000 for clearance 5 over 8 mm);
      XY work-zero transforms are explicitly rejected until B1. 13 tests in
      `tests/sequence_export.rs`.

Changed files: `crates/cam-core/src/checks.rs` (new), `crates/cam-core/src/post/sequence.rs`
(new), `crates/cam-core/src/post/mod.rs` (module wiring), `crates/cam-core/src/sequence.rs`
(`TrustedPlan`), `crates/cam-core/src/lib.rs` (module wiring), additive `PartialEq` derives
in `post/profile.rs` (`M6Contract`, `M6Return`), `crates/cam-core/tests/sequence_export.rs`
(new, 13 tests).

Test evidence: `cargo test -p cam-core --locked --test sequence_export` → 13 passed;
`cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --locked -- -D
warnings` pass; `cargo test --workspace --locked` passed three consecutive times. One
pre-existing flake was observed once in `cam-server` `library_routes_enforce_session_identity_strict_fields_and_metadata_limit`
(it also failed on a clean stashed tree with none of these changes, and passes in every
other run, in isolation and under repetition — unrelated to this work, recorded for
honesty).

Remaining unsupported cases: one ordered program only (sequential split files from
section 14.5 are not implemented); the legacy `post::process()` M5-gated path remains for
the legacy UI and is retired when A4 migrates the adapters; XY work-zero output
transforms and physical stock rectangles arrive in B1 (explicit rejection until then);
`analyze_quality` for sequence plans is not yet wired (legacy V-carve M5 remains the
callable detailed analysis); prefix-scope planning (`through_operation`) is not exposed.

## A4: both adapters — ✅ done (2026-09-09)

Boundary: ui-8 DTOs, native HTTP + WASM dispatch and CLI commands for the canonical
document, plus basic operation-list editing and recovery in the web UI.

Acceptance criteria (from plan section 18):

- [x] One migrated V-carve operation opens, plans and exports in CLI, live UI and WASM.
      - CLI: `cam sequence open|apply-profile|plan|export`
        (`cam-app/tests/sequence_cli.rs`: legacy schema-2 fixture → schema-4 open,
        re-open identity, export blocked on unresolved spindle direction, profile
        application, plan with `--through` prefix scope, program + report written,
        stock-bottom Z-datum verified as machine Z13.000).
      - Native HTTP: `POST /api/v1/sequence` with ui-8 admission
        (`cam-server/tests/api.rs::sequence_route_opens_plans_and_rejects_foreign_versions`).
      - WASM: `sequence(request_json, instance_id)` export through the same envelope
        (`cam-wasm/src/tests.rs::sequence_commands_round_trip_through_the_worker_envelope`;
        the web worker dispatches the `sequence` op, bindings rebuilt).
      - Live UI: `SequenceWorkspace` (schema-4 workspace mode in `App`) opens legacy or
        canonical jobs, edits the operation list, applies a legacy machine profile, plans
        and exports with program download; sequence recovery is versioned independently
        of the legacy tab draft (`web/tests/sequence.test.tsx`, 156 web tests total).
- [x] No supported adapter drops extra operations: planning scopes are explicit
      (`allEnabled` / `throughOperation` prefix), `cam-service/tests/sequence.rs` proves
      two-operation documents plan both under the full scope and exactly one under a
      prefix; unknown scope targets are located errors; capabilities advertise only the
      implemented `flat_vcarve` kind.
- [x] Operation-list editing (rename/enable/move/delete/duplicate) keeps documents valid
      with stable IDs; colliding duplicates and unknown IDs are rejected with located
      diagnostics; every intermediate state validates.

Changed files: `cam-service/src/sequence.rs` (new, ui-8 DTOs + admission + envelope),
`cam-wasm/src/lib.rs` (`sequence` export), `cam-server/src/lib.rs` (route + handler),
`cam-app/src/sequence_cli.rs` (new) + `main.rs` wiring + `Cargo.toml` (cam-service dep,
lockfile updated), `cam-core/src/sequence.rs` (`TrustedPlan::from_generated`),
`cam-core/src/geometry/mod.rs` (`Diagnostic::new`/`at_stage` made public for service
projections), camelCase serde renames on wire-projected core structs, tests:
`cam-service/tests/sequence.rs` (8), `cam-app/tests/sequence_cli.rs` (1),
`cam-server/tests/api.rs` (+1), `cam-wasm/src/tests.rs` (+1). Web:
`src/contracts/sequence.ts` (new), `src/service/sequence.ts` (new http+wasm clients),
`src/service/wasm/worker.ts` (sequence op), `src/components/SequenceWorkspace.tsx` (new),
`src/App.tsx` (workspace mode), `src/styles.css`, `scripts/check-rust-contract.mjs`
(schema-4 + ui-8 drift alarms), regenerated wasm bindings, `tests/sequence.test.tsx` (new).

Test evidence: `cargo test --workspace --locked` 0 failures; `cargo fmt --all -- --check`
and `cargo clippy --workspace --all-targets --locked -- -D warnings` pass;
`pnpm test` 156 passed, `pnpm typecheck`, `pnpm check:contracts`, `pnpm build` (wasm
bindings regenerated) all pass.

Remaining unsupported cases: sequence planning recomputes on every request — retained
task leases/paging beyond the first embedded page arrive with the worker milestone;
the legacy ui-7 transport and six-step workflow remain the primary UI (the sequence
workspace is a separate mode, and 3D stock simulation of sequence plans is deferred to
the timeline slice); edits are service-validated but not undoable in the UI yet;
`PlanScope::ThroughOperation` disables later operations rather than carrying an explicit
prefix identity in results.

## B1: physical stock — ✅ done (2026-09-09)

Boundary: stock dimensions, work-zero resolution (setup origin / stock anchors / custom
points, stock-top / stock-bottom) and the full XYZ output transform.

Acceptance criteria (from plan section 18):

- [x] Center/bottom-zero numeric fixture passes readback.
      (`setup_transform.rs`: stock 100 x 60 x 8 at origin resolves the work zero to
      (50, 30, -8); the plan-section-19.1 numbers hold — setup point (10,20,-2)
      exports as (-40,-10,6) and clearance +5 becomes machine Z +13; a full export
      with that work zero passes the independent numeric readback, asserting the
      emitted Z13.000 clearance block.)
- [x] Origin changes output only. Two jobs differing only in the XY work-zero
      selection produce identical planned motions (and identical removal), while
      the prepared output offsets differ; custom points agree with anchors that
      resolve to the same spot.
- [x] Legacy unknown-XY behavior remains explicit. Migrated jobs keep
      `setup.stock.xy = null`: the setup origin resolves, and selecting a stock
      anchor without physical XY fails with `SETUP_STOCK_XY_REQUIRED`; a
      stock-bottom datum without thickness fails with
      `SETUP_STOCK_THICKNESS_REQUIRED`. The ui-8 document projection exposes the
      stock rectangle, work zero and `physicalXy` flag for previews.

Changed files: `cam-core/src/setup.rs` (new), `cam-core/src/post/sequence.rs` (full
XYZ transform via the resolved work zero; `machine_offset_mm: [x,y,z]` replaces the
Z-only offset; work-zero offsets must be exactly representable at output precision),
`cam-core/src/project.rs` (`AnchorFraction::fraction` public), `cam-service/src/sequence.rs`
(setup block in the document projection), web contract/workspace/tests updated to the
3-axis offset, tests: `cam-core/tests/setup_transform.rs` (new, 4 tests) and the A3
suite adjusted (anchor-without-XY now `SETUP_STOCK_XY_REQUIRED`).

Test evidence: `cargo test --workspace --locked` 0 failures; fmt, clippy -D warnings,
web 156 tests + typecheck pass.

Remaining unsupported cases: the fit action (deriving stock XY from artwork bounds)
and the in-viewport stock rectangle rendering are data/UI conveniences still to
wire (the projection now carries the rectangle); anchor offsets apply to output
only, so previews must render the rectangle in setup coordinates.

## B2: stock prefixes/heights — ✅ done (2026-09-09)

Boundary: ordered removal history, prefix stock identities and concrete height
reference resolution.

Acceptance criteria (from plan section 18):

- [x] Two synthetic stages accumulate removal correctly.
      (`stock_prefixes.rs::two_synthetic_stages_accumulate_removal` and
      `vbit_sweeps_follow_the_conical_removal_model`: `stock::history::StockHistory`
      composes flat-endmill segment sweeps and the conical V-bit model — overlap
      takes the deepest floor, the radius boundary behaves, untouched points stay
      at stock top, cuts clamp at the physical bottom, outside-rectangle queries
      are explicit errors, and the conservative no-cut traverse check accepts a
      clearance transit and rejects one below the remaining floor.)
- [x] Forward refs fail. (`HEIGHT_REFERENCE_FORWARD` from document validation;
      resolution adds `HEIGHT_REFERENCE_UNRESOLVED` for face planes not yet
      established, `HEIGHT_RANGE_INVALID` for bottoms at or above tops, and
      `SETUP_STOCK_THICKNESS_REQUIRED` — no silent stock-top substitution.)
- [x] Changing an earlier operation stales the suffix. Prefix stock ids follow
      plan section 9.2: the initial view hashes stock/artwork/planning inputs
      once, and each operation extends it with its settings, used tool snapshots
      and actual execution. Editing operation one's depth changes its after-id
      and every later before/after id while the unchanged operation keeps its
      stage identity. `OperationPlan::stock_history(through)` builds the ordered
      batches from the executed prefix (milling sweeps only; knife traces never
      enter), and the real m3 plan's history shows the carved floor at depth 2
      inside the selection and untouched material outside it.

Changed files: `cam-core/src/stock/history.rs` (new), `cam-core/src/stock.rs`
(module wiring), `cam-core/src/setup.rs` (`resolve_heights` with the
OperationTop-with-offset fix), `cam-core/src/sequence.rs` (prefix stock ids,
`stock_history` builder), `cam-core/tests/stock_prefixes.rs` (new, 5 tests).

Test evidence: `cargo test --workspace --locked` 0 failures; fmt, clippy -D
warnings, web tests (156), typecheck, contract checks pass.

Remaining unsupported cases: `established_planar_region` coverage proofs and
FaceResult plane publication arrive with C1/C2; per-operation invalidation is
still whole-sequence replanning (the prefix ids now make finer invalidation
possible later); the traverse check samples the corridor conservatively rather
than exact corridor geometry.

## C1: face core — ✅ done (2026-09-10)

Boundary: rectangle raster at 0/90 degrees, depth layers, extensions, entry logic
and coverage checks, with the face plane published as a named output.

Acceptance criteria (from plan section 18):

- [x] Face-only job without SVG has complete coverage and valid transitions,
      including non-plunging entry outside actual stock.
      (`face_core.rs::face_only_job_without_svg_plans_completely`: no source, one
      face operation, basic checks pass, `stock_history` sampling shows the whole
      faced floor at the bottom with outside-stock queries explicit;
      `interior_face_plunges_and_outside_stock_face_enters_air`: interior faces
      plunge with feed and milling effect, while a rectangle extending past the
      physical stock enters wholly-outside air with a rapid, feed-less, non-cutting
      descent.)
- [x] Extra fixtures: awkward width/stepover keeps the last strip and corners
      covered (an explicit final row beats integer division); 90-degree passes
      rotate the raster with the same coverage proof; depth layers end exactly at
      the bottom and repeat the pattern per layer; one-way retracts between rows
      while zigzag alternates direction and links rows as checked cutting moves
      with feeds; margins expand coverage and overruns extend travel separately;
      unsupported angles (`FACE_ANGLE_UNSUPPORTED`), oversized stepovers
      (`FACE_STEPOVER_RANGE`/`FACE_COVERAGE_INCOMPLETE`) and missing physical
      dimensions (`SETUP_STOCK_XY_REQUIRED` surfaced as a located missing field)
      are explicit diagnostics. The allowed envelope is derived from settings
      (coverage + overruns + radius + reserve) and generated paths are checked
      against it (`FACE_ENVELOPE_EXCEEDED` guards self-authorizing bugs).

Changed files: `cam-core/src/operations/face.rs` (new planner),
`cam-core/src/operations/mod.rs` (dispatch), `cam-core/src/sequence.rs` (face
allowed in plan_job; `NamedOutput` carries the face-plane Z and covered
rectangle; `PlannedOperation.named_outputs` threaded into results),
`cam-service/src/sequence.rs` (face missing-field projection; capabilities
advertise `face`), tests: `cam-core/tests/face_core.rs` (new, 8 tests),
`sequence_plan.rs` unsupported-kind test moved to profile (still unplanned),
`cam-service/tests/sequence.rs` capabilities expectation.

Test evidence: `cargo test --workspace --locked` 0 failures; fmt, clippy -D
warnings pass (web suite unchanged: 156).

Remaining unsupported cases: arbitrary pass angles (only 0/90); imported-region
face areas (rectangles only); FaceResult consumption by later operations and the
uniform-top V-carve adapter ship in C2; the UI face editor is C2's workflow slice.


## C2: face workflow — ✅ done (2026-09-10)

Boundary: face outputs consumed by later operations, the uniform-top Flat V-carve
adapter in its local frame, and UI area/margin controls through a strict engine
UpdateSettings command. The rotated scan helper is not advertised (C1 supports
0/90° only) and is therefore not part of this slice.

Acceptance criteria (from plan section 18):

- [x] Face -> V-carve fixture uses the faced plane and original work zero
      correctly in simulation and numeric output.
      (`face_workflow.rs::face_then_carve_uses_the_faced_plane_and_original_work_zero`:
      face 0.5 then carve 2 below it — the face plane publishes at Z=-0.5, carve
      top resolves to -0.5, deepest cut exactly -2.5, clearance stays at the
      original +5 plane (not local +5.5); the composed `stock_history` shows the
      faced floor -0.5 outside the carve and the carved floor -2.5 inside it
      (the material model simulation consumes); center/bottom-zero output passes
      the independent numeric readback with the deepest cut at machine Z5.500
      and clearance at Z13.000.)
- [x] Partial-face reference crossing is rejected.
      (`partial_face_reference_crossing_is_rejected`: a half-width face with a
      full-width carve is `incomplete` with `SURFACE_REFERENCE_OUTSIDE_COVERAGE`,
      the face itself stays complete, and the sequence is not export-ready;
      `earlier_removal_below_the_faced_plane_is_rejected` adds the second
      admission rule — earlier milling below the faced plane inside the target
      invalidates the fresh-planar assumption. `stock_top_carve_keeps_legacy_coordinates`
      is the legacy-equivalence regression.)
- [x] Behavior details: `FlatVcarveSettings.top` (default stock top, serde
  default keeps old documents valid; migration preserves stock-top behavior);
  face planes publish into the sequence walk and resolve through
  `setup::resolve_top`; the adapter shifts stock thickness, clearance and every
  returned motion consistently (plan section 13.2) instead of shifting output
  motions alone; execution accounting accepts consecutive same-tool stages
  (face T1 -> carve T1 share one tool change) while recurring tools are still
  re-selected in order; the emitted writer keeps one M6 group per stage with
  machine-owned bridge positioning that the independent readback skips exactly
  (`post/sequence.rs`); cam-service gains `UpdateSettings` (strict
  `OperationSettings` parse + full document revalidation, unknown operations
  and unknown fields located); the web workspace exposes a face settings editor
  (rectangle, margins, raster, overruns, pattern) that only ever submits through
  the engine inside the adjacently tagged kind envelope.

Changed files: `cam-core/src/project.rs` (`FlatVcarveSettings.top`, HeightRef
defaults), `cam-core/src/project/migrate.rs`, `cam-core/src/setup.rs`
(`resolve_top`), `cam-core/src/operations/mod.rs` (`PublishedFace`,
published-faces + prior-motions plumbing), `cam-core/src/operations/flat_vcarve.rs`
(local-frame adapter + uniform-top admission), `cam-core/src/sequence.rs`
(face-plane publication), `cam-core/src/checks.rs` (same-tool consecutive stage
accounting), `cam-core/src/post/sequence.rs` (bridge-skip readback),
`cam-service/src/sequence.rs` (`UpdateSettings`), web:
`src/contracts/sequence.ts`, `src/service/sequence.ts` (updateSettings),
`src/components/FaceSettingsEditor.tsx` (new), `src/components/SequenceWorkspace.tsx`
(select-operation inspector). Tests: `cam-core/tests/face_workflow.rs` (new, 4),
`cam-service/tests/sequence.rs` (+1), `web/tests/sequence.test.tsx` (+2).

Test evidence: `cargo test -p cam-core --locked --test face_workflow` → 4 passed;
`cargo test --workspace --locked` 0 failures; `cargo fmt --all -- --check`,
`cargo clippy --workspace --all-targets --locked -- -D warnings` pass; web 158
tests, `typecheck`, `check:contracts` pass.

Remaining unsupported cases: the in-browser 3D timeline/stock rendering of
sequence plans is still deferred (D3, as recorded in A4) — the planning-side
material history the simulation will consume is covered here; arbitrary pass
angles remain unadvertised (0/90 only, C1); V-carve after a face still excludes
overlapping prior non-face work inside the target (conservative admission);
`UpdateSettings` edits are not undoable in the UI yet.

## D1: contour catalogue — ✅ done (2026-09-10)

Boundary: `contours.rs` builds stable closed-contour identity over the imported
artwork — roles (outer/hole with lineage), canonical winding-independent
geometry, placement-independent source fingerprints, and anchor resolution.
No planner or UI selection consumes it yet within this slice (profile does in
D2; UI pickers in D3).

Acceptance criteria (from plan section 18):

- [x] Reversing source winding preserves compensation meaning.
      (`reversed_source_winding_preserves_compensation_meaning`: identical
      contour IDs, roles, lineage, suggested sides, canonical vertex
      sequences, fingerprints and enclosed-region areas for every-subpath-
      reversed artwork. Canonical form: CCW from the lexicographically
      smallest vertex, so compensation sides are computed on winding-free
      geometry and traversal stays a separate planner choice.)
- [x] Selecting one hole does not select every boundary.
      (`selecting_one_hole_does_not_select_every_boundary`: one hole ID
      selects exactly that hole — never the enclosing outer or the sibling
      hole — contrasting the flat-vcarve component union; unknown references
      fail with `CONTOUR_REFERENCE`.)
- [x] Placement moves anchors correctly.
      (`placement_moves_anchors_correctly`: the same anchor (contour +
      fingerprint + fraction) resolves under identity and under scale 2 /
      rotation 90° placements to the transformed point (within 1e-6), the
      fraction addresses the same source location, and the tangent rotates
      with the placement. Fractions parameterize the placement-independent
      page-space canonical ring, quantized keys absorb float noise.)
- [x] Extra contracts: source edits leave anchors explicitly unresolved
      (`CONTOUR_ANCHOR_UNRESOLVED`, no nearest-contour guessing —
      `source_geometry_edits_leave_anchors_unresolved`); jobs without an SVG
      source have no catalogue (`PROJECT_SOURCE_REQUIRED`); contour IDs use
      the document identifier alphabet with deterministic hole ordering by
      page-space geometry and a build-time collision check.

Changed files: `cam-core/src/contours.rs` (new), `cam-core/src/lib.rs` (module
wiring), `cam-core/tests/contour_catalogue.rs` (new, 5 tests).

Test evidence: `cargo test -p cam-core --locked --test contour_catalogue` →
5 passed; full `cargo test --workspace --locked` 0 failures; fmt and
`cargo clippy --workspace --all-targets --locked -- -D warnings` pass; web
suite unchanged (158 tests), typecheck and contract checks pass.

Remaining unsupported cases: open chains (`ContourRole::Open` is reserved for
the knife import milestone F1); the catalogue is not yet projected through
cam-service/UI (D3); anchor fits (e.g. tab space) are checked by their
owning features; fingerprints quantize at 0.01 mm so sub-10 µm artwork edits
are indistinguishable (documented in the module).

## D2: basic profile — ✅ done (2026-09-10)

Boundary: the profile planner (`operations/profile.rs`) — explicit inside/
outside/on compensation per contour, climb/conventional traversal derived
from the retained side and spindle rotation, depth passes ending exactly at
the bottom, explicit through-cut permission, deterministic automatic start
and inner-before-outer ordering, and a simple plunge entry. One endmill; no
V-bit anywhere.

Acceptance criteria (from plan section 18):

- [x] Inside/outside circle fixtures have expected center radius, depth and
      sequence. (`circle_fixture_has_expected_center_radius_for_every_side`:
      radius-20 circle, cutter radius 3 — outside center radius 23, inside
      17, on-contour 20, all cut motions within 0.02 mm; single-stage plan
      with the endmill as the only tool. `nested_cutouts_cut_inner_first_...`:
      frame-with-hole plus an isle inside the hole cut in the order
      isle → frame hole → frame outer.)
- [x] Depth and sequence with through allowance: 8 mm stock, depth 8.2,
      stepdown 3 cuts layers ending exactly at -3, -6, -8.2 per contour with
      allowance 0.2 as explicit below-stock permission
      (`PROFILE_THROUGH_ALLOWANCE` guards requests beyond it); basic checks
      pass and the plan is export-ready.
- [x] New profiles need no V-bit: every fixture job carries exactly one
      endmill tool and plans completely.
- [x] Extra fixtures: `cut_direction_and_spindle_decide_traversal` — the
      four climb/conventional × CW/CCW combinations produce the signed loop
      orientation that keeps retained material on the feed-left side for
      climb-with-clockwise (material-side rule per contour role and side;
      on-contour traversals use the explicit direction);
      `reversed_source_winding_keeps_retained_side_and_order` — reversed
      artwork yields identical motion geometry in identical order;
      `offset_collapse_is_diagnosed_not_skipped` — a radius-2 circle with a
      radius-3 cutter inside collapses to `PROFILE_OFFSET_UNAVAILABLE`
      naming the contour, never a silent skip;
      `missing_direction_and_later_slice_features_are_explicit` — missing
      climb/conventional is a located missing field; ramp entry, anchor
      starts, finishing, tabs and leads produce their specific
      unsupported-feature diagnostics; unknown contour references are
      located. Offset splits retain provenance (every result loop is cut
      under the source contour's identity).

Changed files: `cam-core/src/operations/profile.rs` (new),
`cam-core/src/operations/mod.rs` (dispatch + published-face plane map),
`cam-core/src/sequence.rs` (profile admitted in `plan_job`),
`cam-service/src/sequence.rs` (capabilities advertise `profile`),
`cam-core/tests/profile_basic.rs` (new, 6 tests),
`cam-core/tests/sequence_plan.rs` (unsupported-kind fixture moved to the
knife operation), `cam-service/tests/sequence.rs` (capabilities).

Test evidence: `cargo test -p cam-core --locked --test profile_basic` →
6 passed; full `cargo test --workspace --locked` 0 failures; fmt and clippy
`-D warnings` pass; web 158 tests, typecheck, `check:contracts` pass.

Remaining unsupported cases: finishing allowances/passes (E2), tabs (E1),
ramps/leads/anchor starts (E3) — each diagnosed explicitly today; inner-side
compensation of an outer contour ignores sibling holes in the enclosed
region until pockets ship (G); retained-geometry sweep checks and entry/
retract stock checks are the basic checks plus layer-exactness for now and
deepen with E3; UI profile editing is D3.

## D3: profile workflow — ✅ done (2026-09-10)

Boundary: profile height references admitted only inside faced coverage, the
plan-section-19.1 mixed fixture (Face T1 -> V-carve T1 rough/T2 finish ->
Profile T1) through both adapters, ui-8 motion paging + contour catalogue
projection, and the web stock/timeline display of sequence plans with
per-operation ownership. UI profile editing covers contour selection with
explicit sides, heights, stepdown, through allowance, direction and the
milling assignment.

Acceptance criteria (from plan section 18):

- [x] Face -> V-carve -> Profile with a recurring tool preserves
      tool/process/order and complete material history in both adapters.
      (`profile_workflow.rs::face_carve_profile_recurring_tool_preserves_process_order_and_stock_history`:
      stages face/vcarve_rough/vcarve_finish/profile_rough with tools
      endmill/endmill/vbit/endmill in order; execution re-selects the
      endmill after the V-bit (`ToolChange endmill → vbit → endmill`), one
      process intent per stage run; the through profile's layers end exactly
      at -3.5/-6.5/-8.2 below the faced top; the composed `stock_history`
      shows the faced floor, the carved floor and the through-cut corridor
      clamped at the stock bottom; export emits M6 groups T1,T1,T2,T1 in
      execution order and passes the independent numeric readback. The same
      fixture runs through the ui-8 service projection
      (`cam-service/tests/sequence.rs::profile_workflow_commands_plan_page_and_catalogue_the_mixed_fixture`)
      and the new `motions`/`contours` commands round-trip through the WASM
      worker envelope (`cam-wasm/src/tests.rs`).)
- [x] UI profile editing: `ProfileSettingsEditor` (engine catalogue list
      with per-contour inside/outside/on, suggested defaults, stock-top or
      preceding-face-plane tops, depth-below-top bottoms, stepdown, through
      allowance, climb/conventional, feeds/RPM/spindle direction) submits
      only through `UpdateSettings` inside the `profile` kind envelope;
      profile missing-field resolution now projects per field
      (`missing_by_operation`); an `add` operation edit creates incomplete
      face/profile operations bound to an explicit tool (nothing invented,
      `add_operation_edits_create_incomplete_face_and_profile_operations`).
- [x] Stock/timeline display: `sim/sequenceSim.ts` consumes the complete
      ordered motion stream (paged via the new `Motions` command until
      `total`; explicit incomplete state on stall or over the 200k-motion
      cap), maps purposes onto engine kinds (knife traces never cut), keeps
      8-bit tool indices with an explicit 256-tool cap and 16-bit operation
      ownership, and tracks per-operation removed volume by depth diffs
      across each cutting motion's scan box. `SequenceTimeline` renders the
      top-down stock view (depth ramp, distinct through-cut color, optional
      operation coloring), captures a checkpoint after every operation and
      supports exact scrub/rewind through checkpoint restore
      (`web/tests/sim/sequenceSim.test.ts`, `web/tests/sequence.test.tsx`).

Changed files: `cam-core/src/operations/profile.rs` (published-face map +
coverage admission), `cam-core/src/operations/mod.rs` (face map passed
whole), `cam-service/src/sequence.rs` (profile missing fields, `Motions`,
`Contours`, `Add` edit, capability flags), tests: `cam-core/tests/profile_workflow.rs`
(new, 3), `cam-service/tests/sequence.rs` (+2), `cam-wasm/src/tests.rs`
(extended). Web: `src/contracts/sequence.ts`, `src/service/sequence.ts`,
`src/sim/sequenceSim.ts` (new), `src/components/ProfileSettingsEditor.tsx`
(new), `src/components/SequenceTimeline.tsx` (new),
`src/components/SequenceWorkspace.tsx`, `src/styles.css`,
`tests/sequence.test.tsx`, `tests/sim/sequenceSim.test.ts` (new).

Test evidence: `cargo test -p cam-core --locked --test profile_workflow` →
3 passed; full `cargo test --workspace --locked` 0 failures (48 targets);
`cargo fmt --all -- --check` and clippy `-D warnings` pass; web 166 tests,
`typecheck`, `check:contracts`, `pnpm build` (wasm bindings) pass.

Remaining unsupported cases: timeline rendering is a 2D top-down view (the
3D WebGL viewport stays with the legacy workflow until the sequence sim
replaces it); timeline checkpoints are per operation, not per stage/pass;
`Motions` re-plans on every page request (retained-task leases remain the
later worker milestone); drag-knife tools are excluded from the milling
field (their trace display ships with F); the profile editor exposes the
D2 feature set (tabs/finishing/leads/ramps editors arrive with E1-E3).

## E1: tabs — ✅ done (2026-09-10)

Boundary: `operations/profile/tabs.rs` — protected tab footprints (bridge
dilated by the cutter radius plus margin into a restricted centerline
interval), rectangular tabs in automatic (count/spacing, round-robin over
eligible straight runs) and manual (source-geometry anchors) placement, the
commanded-depth envelope `z(s) = max(pass_z, tab_top)` split exactly at
boundaries with feed-controlled vertical rise/lower moves, tab tops measured
from the physical stock bottom, and resolved placements stored in the plan.
Ramped shoulders are diagnosed (`PROFILE_TAB_SHAPE_UNSUPPORTED`) and stay
unadvertised (`rampedTabs: false`).

Acceptance criteria (from plan section 18):

- [x] Actual sweeps retain minimum tab width/height, including through
      allowance and every depth pass.
      (`profile_tabs.rs::automatic_tabs_hold_the_bridge_in_every_deep_pass`:
      8 mm stock, cut bottom -8.2 (allowance 0.2), stepdown 2 → layers
      -2/-4/-6/-8.2; tab height 2 → bridge top exactly -6 regardless of the
      -8.2 bottom. The composed stock history probes all four corridor
      midpoints: exactly the two tabbed sides hold -6 with their 5 mm bridges
      (interior ±1.75 stays, beyond ±3.1 cuts through to the clamped -8
      bottom), the other two cut through. Tab transitions appear in every
      deep pass; the -2/-4 passes stay unsplit. The tabbed plan passes basic
      checks and exports through the independent numeric readback with tab
      tops at machine Z-6.000.)
- [x] Impossible count/corner placement reports a located error.
      (`impossible_tab_count_reports_the_deficit`: 500 requested, the deficit
      and the fitting count are in the message — never silently fewer;
      `curved_contour_cannot_host_straight_span_tabs`: a circle has no
      sufficiently long straight span, naming the contour;
      `manual_tab_anchor_holds_material_at_the_anchor`: an anchor whose
      bridge runs past a corner is `PROFILE_TAB_NO_SPACE` "runs out of room",
      while a mid-edge anchor holds its material at the tab top.)
- [x] Extra contracts: heights ≥ stock are `PROFILE_TAB_RANGE`; tabs below
      every pass are `PROFILE_TAB_INEFFECTIVE`; missing height/width are
      located missing fields; footprints that would consume the whole
      contour are rejected; overlapping manual footprints merge
      conservatively; `NamedOutput.tab_placements`
      (`profile_tabs`, camelCase, strict) carries bridge/restricted intervals
      and the top Z into the plan document and the ui-8 operations
      projection; tab-free profiles generate byte-identical motions to D2
      (`profile_basic.rs` unchanged and passing).

Changed files: `cam-core/src/operations/profile.rs` →
`cam-core/src/operations/profile/{mod.rs,tabs.rs}` (planner + tab geometry),
`cam-core/src/sequence.rs` (`TabPlacementOutput` on `NamedOutput`),
`cam-core/src/operations/face.rs` (output construction),
`cam-service/src/sequence.rs` (namedOutputs in the operations projection),
`web/src/contracts/sequence.ts` (namedOutputSchema), tests:
`cam-core/tests/profile_tabs.rs` (new, 7), `web/tests/sequence.test.tsx`
(fixture field).

Test evidence: `cargo test -p cam-core --locked --test profile_tabs` →
7 passed; full `cargo test --workspace --locked` 0 failures (49 targets);
fmt, clippy `-D warnings`, web 166 tests, `typecheck`, `check:contracts`,
`pnpm build` pass.

Remaining unsupported cases: ramped tab shoulders (diagnosed; advertised only
when implemented); tabs restricted to sufficiently long straight spans in
this release (curved contours report `PROFILE_TAB_NO_SPACE`); per-contour
tab overrides (one TabSettings per operation); the tab editor UI and
timeline tab overlay arrive with E3's inspection work.

## E2: finishing — ✅ done (2026-09-10)

Boundary: radial finishing (plan section 10.2) — the rough centerline offset
carries the radial allowance (R + a), one final pass runs the exact offset (R)
depth-stepped like the rough work with its own feed, per-contour
rough-then-finish staging with contiguous same-role stage runs, and tabs
resolved independently for each loop's arc space so every deep rough and
finish pass holds the same protected bridges. Legacy V-carve wall allowances
are untouched. Leads, ramps and anchor starts remain E3 diagnostics.

Acceptance criteria (from plan section 18):

- [x] Rough offset and final offset match requested allowance.
      (`profile_finish.rs::rough_and_finish_offsets_match_the_requested_allowance`:
      the plan-section-19.1 circle fixture — radius 20, cutter radius 3,
      allowance 0.5 — rough center radius 23.5 outside / 16.5 inside, final
      23 / 17, all within 0.02 mm; two consecutive same-tool stages
      (`profile-1-profile-rough` then `...-finish`, one tool change overall);
      finish layers end exactly at the bottom; rough motions carry the
      cutting feed and finish motions the finishing feed. Finish-disabled
      profiles keep the D2 single-rough-stage shape with no finish motions.)
- [x] Finish motion preserves all tabs.
      (`finish_passes_protect_every_tab`: the E1 tab fixture plus a 0.5 mm
      allowance — both the rough and the finish stage carry tab transitions
      held at the -6 tab top in every deep pass, and the composed stock
      history keeps the two bridges (interior ±1.75 mm stays -6) while the
      other two sides cut through to -8; all finish motions carry the
      finishing feed.)
- [x] No change to legacy V-carve allowances.
      (Legacy suites run unchanged — sequence_plan legacy-equivalence,
      face_workflow, vcarve adapter and profile_basic geometry fixtures;
      document validation rejects a negative allowance or non-positive finish
      feed at editing time with `PROJECT_PARAMETER` naming the field, so
      supplied garbage never reaches the planner.)
- [x] Extra contracts: zero allowance is a meaningful supplied value — the
      finish pass runs at the exact final offset and serde keeps
      `Some(0.0)` distinct from unset
      (`zero_allowance_finish_is_meaningful`); missing allowance/feed are
      located missing fields (`finish.radial_allowance_mm`,
      `finish.feed_mm_min`, zero allowance not missing); on-contour
      selections with a positive allowance are `PROFILE_FINISH_ALLOWANCE`
      (zero stays allowed); a contour's complete rough work precedes its
      finish work before the next contour (nesting order preserved); stage
      dilation protects the bridge by the cutter disc radius while manual
      anchors match their loop's compensation distance; capabilities
      advertise `profileFinishing: true`.

Changed files: `crates/cam-core/src/operations/profile/{mod.rs,tabs.rs}`
(rough/finish loop construction at R+a / R, shared `emit_loop_layers`
emission, tab span resolution split into anchor offset vs cutter-radius
dilation), `crates/cam-service/src/sequence.rs` (capability flag), tests:
`crates/cam-core/tests/profile_finish.rs` (new, 4 tests).

Test evidence: `cargo test -p cam-core --locked --test profile_finish` → 4
passed; full `cargo test --workspace --locked` 0 failures (50 targets);
fmt, clippy `-D warnings`, web 166 tests, `typecheck`, `check:contracts`
pass.

Remaining unsupported cases: multiple radial finish passes, separate axial
allowance, a named spring-pass mode and full-height wall finishing are later
additions (plan section 10.1); the finish entry is still a plunge and lead
moves are absent (E3); the profile editor does not yet expose finish fields
(they pass through `UpdateSettings` opaquely today; the editor grows them
with E3's inspection work).

## E3: entries/inspection — ✅ done (2026-09-10)

Boundary: `operations/profile/entries.rs` + the planner's entry/lead/start
resolution — contour ramp entries descending along the compensated path
(lead-in first, then the loop, wrapping past the seam to flatten the ramped
section), tangent line/arc leads with their own feeds checked against the
retained side of every selected contour, anchor starts repositioning the
loop seam, automatic tab placement excluding the entry/lead neighborhood,
and manual placements rejecting lead/ramp crossings. UI: the profile editor
grows entry/lead/start/tabs/finish controls and the timeline gains an exact
tab-bridge overlay plus a depth probe with its resolution. Ramped tab
shoulders stay diagnosed; default (plunge, no lead, automatic start)
profiles stay byte-identical to E2.

Acceptance criteria (from plan section 18):

- [x] Leads and ramps pass the retained-geometry/tab checks; narrow invalid
      entry is rejected. (`profile_entries.rs`: arc leads arrive tangent at
      the seam, bulge to the scrap side, stay within the chord-error budget
      and no closer to the retained wall than the loop itself
      (`arc_leads_bulge_to_the_scrap_side_within_tolerance`); tangent line
      leads blend at the seam with their own feeds and cut their corridor
      (`tangent_line_leads_blend_with_their_own_feeds`); the inside-circle
      line lead — every point beyond the tangency comes closer than the
      cutter radius to the wall — is `PROFILE_LEAD_NO_SPACE` naming the
      contour, with no motions (`narrow_invalid_entry_is_rejected`); the
      plan-section-19.1 combined fixture (tabs count 2 + finish 0.5 + ramp
      45° + line leads) plans completely, both rough and finish stages hold
      tab transitions at −6, no Entry/LeadIn/LeadOut motion crosses any
      resolved bridge footprint, the stock history keeps exactly the two
      bridged sides at −6, and the program exports through the independent
      numeric readback with every feed preserved
      (`tabs_finish_ramp_and_leads_coexist`).)
- [x] Blocked ramps do not become plunges. A ramp must fit one continuous,
      tab-free path section: a manual tab on the ramp span and a ramp longer
      than one revolution are `PROFILE_RAMP_NO_SPACE` naming the contour,
      with zero emitted motions — never a plunge fallback
      (`blocked_ramps_do_not_become_plunges`); every emitted entry segment
      obeys the angle and the traversal wraps to flatten the ramped floor
      (`ramp_entry_descends_along_the_contour_within_the_angle`, probing the
      composed stock history through the ramped span).
- [x] Mixed milling fixture is usable from UI through export.
      (`cam-service/tests/sequence.rs::profile_entries_plan_and_export_through_the_service`:
      the D3 face→carve→profile fixture's profile is rewritten through the
      strict UpdateSettings command with a ramp entry, line leads, tabs,
      finishing and an anchor start bound to the catalogue fingerprint; all
      operations plan complete, paged motions carry entry/lead_in/lead_out/
      tab_transition/rough/finish purposes, tab placements carry drawable
      four-corner footprints, and the export passes readback with the ramp,
      lead and finishing feeds. The web editor submits the same shapes
      inside the profile kind envelope (`web/tests/sequence.test.tsx`,
      168 web tests), and the timeline renders the tab-bridge overlay and
      depth probe (`web/tests/sim/sequenceSim.test.ts`).)
- [x] Extra contracts: anchor starts resolve through the source fingerprint
      (fraction-parameterized; foreign contours are `CONTOUR_REFERENCE`,
      stale fingerprints `CONTOUR_ANCHOR_UNRESOLVED`
      — `anchor_start_moves_the_seam`); ramps require an explicitly
      ramp-capable tool (`PROFILE_ENTRY_CAPABILITY`; the unset capability is
      a located missing field) and angle/feed/lead values are located
      missing fields; automatic placement excludes the seam neighborhood
      and an impossible count reports the entry/lead restriction
      (`automatic_tabs_exclude_the_entry_neighborhood`); arc leads on
      on-contour selections are `PROFILE_LEAD_SIDE`;
      `TabPlacementOutput.footprintMm` carries the exact bridge quad
      (optional on parse so older plans load); capabilities advertise
      `profileEntries: true`; E1/E2 fixtures are unchanged and green.

Changed files: `crates/cam-core/src/operations/profile/entries.rs` (new:
lead shapes, arc linearization within half the motion tolerance, exact
segment/ring retained-geometry checks), `crates/cam-core/src/operations/
profile/mod.rs` (entry/lead/start resolution, ramp+wrap emission, anchor
seams, lead checks), `crates/cam-core/src/operations/profile/tabs.rs`
(exclusion windows in automatic placement, bridge footprints, arc-point
helpers), `crates/cam-core/src/sequence.rs` (`footprint_mm` on
`TabPlacementOutput`), `crates/cam-service/src/sequence.rs`
(`profileEntries` capability, contour `sourceFingerprint` projection),
tests: `crates/cam-core/tests/profile_entries.rs` (new, 9),
`profile_basic.rs` (the D2 later-slice fixture now expects the capability
diagnostic), `profile_tabs.rs` (strict wire test covers `footprintMm`),
`crates/cam-service/tests/sequence.rs` (E3 service round-trip, capability
flag, ramp-capable fixture tool). Web: `src/contracts/sequence.ts`
(`footprintMm`, `sourceFingerprint`), `src/components/
ProfileSettingsEditor.tsx` (entry/lead/start/tabs/finish editors),
`src/components/SequenceTimeline.tsx` (tab overlay + depth probe),
`src/sim/sequenceSim.ts` (`probeDepth`), `src/styles.css`,
`tests/sequence.test.tsx`, `tests/sim/sequenceSim.test.ts`.

Test evidence: `cargo test -p cam-core --locked --test profile_entries` →
9 passed; full `cargo test --workspace --locked` 0 failures (51 test
targets); `cargo fmt --all -- --check` and clippy `-D warnings` pass; web
168 tests, `typecheck`, `check:contracts`, `pnpm build` (wasm bindings)
pass.

Remaining unsupported cases: multi-revolution/helical ramps (a ramp must
fit one revolution without crossing a tab); ramped tab shoulders remain
diagnosed (E1); arc leads on on-contour selections are rejected; lead
retained-geometry checks cover this operation's selected contours only
(unselected neighbours and stock edges are not lead-aware); arc leads are
linearized G1 polylines (native G2/G3 is a later writer change); when a
ramp covers the whole lead-in, the descent owns that path and no separate
lead feed appears; anchor starts pick the nearest split loop when an
offset splits; the editor exposes automatic tab placement only (manual
anchors stay a document-level feature) and no drag handles; the timeline
remains a 2D top-down view.

## F1: knife data/import — ✅ done (2026-09-10)

Boundary: knife tool geometry and typed presets in the shared tool library
(swivel feed instead of stepover, no spindle speed), a separate centerline
import mode producing open chains in the contour catalogue (open subpaths
stay open, stroke widths ignored as centerlines, source order preserved),
knife-stage process preparation (spindle/coolant off, exact path) through
writer and independent readback, and the pivot/tip display contract (modeled
blade headings on knife motions plus the tip-derivation helper). The knife
operation planner itself ships in F2 and stays unadvertised.

Acceptance criteria (from plan section 18):

- [x] Open paths stay open; stroke centerlines are not doubled.
      (`knife_import.rs`: centerline mode imports `M…L…` subpaths as chains
      whose endpoints stay distinct in document and subpath order
      (`open_paths_stay_open_and_subpath_order_is_preserved`); a stroked
      path yields exactly one centerline of the drawn length — the stroke
      width is ignored, never offset into two parallel cuts — with the
      interpretation visible as an `SVG_STROKE_CENTERLINE` diagnostic naming
      the element (`stroke_centerlines_are_not_doubled`); explicit `Z`
      subpaths, `line`, `polyline` (never implicitly closed; its perimeter
      is the drawn length) and `polygon` import with the right closure
      flags; filled elements keep importing as contours beside chains, while
      a fill+stroke element is an explicit separation error; the fill
      importer is untouched — the same sources still fail with `SVG_STROKE`
      /`SVG_OPEN_PATH` in fill mode; placement moves chains and their
      anchors with placement-independent fingerprints, and near-zero jogs
      merge only within the declared tolerance/4 budget while near-reversal
      corners survive for knife orientation.)
- [x] Tool changes do not turn the spindle on for the knife.
      (`knife_stage.rs`: a hand-assembled face → knife → face plan —
      standing in for F2's planner output at the exact sequence contract —
      prepares the knife stage with spindle off, coolant off and exact path
      even under a blend/flood profile; the writer emits explicit M5/M9
      after the knife M6 followed by that stage's G61 modal group, no
      M3/M4/M7/M8 appears anywhere in the knife block, and the later milling
      stage restores its own M3/M8; the independent readback now tracks
      spindle and coolant state per stage and rejects injected `M3`/`M8`
      during knife motion with `PROCESS_SPINDLE_STATE`/`PROCESS_COOLANT_STATE`.)
- [x] No unsupported knife planner is advertised.
      (`capabilities_advertise_only_implemented_features` keeps `drag_knife`
      out of `operationKinds`; `plan_job` rejects an enabled knife operation
      with `OPERATION_PLANNER_UNAVAILABLE` naming it while a disabled knife
      operation plans the rest of the job
      (`knife_stage.rs::the_knife_planner_stays_unadvertised_and_unplanned`,
      `cam-service` Plan round-trip); `openContours`/`knifeToolLibrary`
      flip true as the import and library data really exist, `knifeReplay`
      stays false.)
- [x] Extra contracts: knife library tools validate and round-trip strictly
      (milling presets on knife tools, knife presets on milling tools and
      milling entry capabilities on knife tools are `LIBRARY_TOOL_KIND`;
      knife preset JSON rejects `spindle_rpm`/`stepover_mm` outright), knife
      preset CRUD is an immutable transaction, `apply_knife_to_job` copies
      geometry and preset feeds into exactly one DragKnife operation
      (sibling operations using the same tool stay untouched) and never into
      a legacy job slot. Knife motions carry modeled blade headings
      `(start, end)` in degrees CCW from +X (basic checks add
      `PLAN_KNIFE_HEADING` when missing on knife stages or present on
      milling stages; `KnifeTrace` effects and knife purposes are confined
      to knife stages); programmed knife XY is the holder pivot and
      `toolpath::knife_tip` derives the visible tip — the
      plan-section-19.1 corner numbers (pivot (11,0) heading 180° → tip
      (10,0)) hold. The ui-8 `Contours` command projects `openChains` (role
      "open", suggestedSide "on"), motion pages may carry `bladeHeadingDeg`,
      and the web exposes knife tool geometry (`knifeTools`) for pivot/tip
      trace display while knife traces still never cut the heightfield.

Changed files: `crates/cam-core/src/svg/{mod.rs,path.rs}` (import mode, chain
flattening/extraction, `ImportMode` omitted while default so legacy documents
stay byte-identical), `crates/cam-core/src/contours.rs` (open-chain catalogue
entries with placement/simplification, `select_chains`, open-aware anchors),
`crates/cam-core/src/toolpath.rs` (`blade_heading_deg`, `knife_tip`),
`crates/cam-core/src/checks.rs` (knife heading/effect/purpose role checks),
`crates/cam-core/src/post/sequence.rs` (knife coolant/exact-path
preparation, per-stage modal group, coolant readback),
`crates/cam-core/src/tool_library.rs` (knife geometry + typed presets, CRUD,
`apply_knife_to_job`), `crates/cam-core/src/project.rs` (`DragKnifeSpec`
validation shared), planner motion constructors updated for the heading field
(`operations/{face.rs,flat_vcarve.rs,profile/mod.rs}`),
`crates/cam-service/src/sequence.rs` (`openChains` projection, capability
flags), tests: `crates/cam-core/tests/knife_import.rs` (new, 6),
`knife_stage.rs` (new, 4), `tool_library.rs` (+3 knife, 14 total),
`crates/cam-service/tests/sequence.rs` (+1). Web: `src/contracts/{job,
sequence,library}.ts` (import mode, `bladeHeadingDeg`, `openChains`, knife
library schema), `src/sim/sequenceSim.ts` (`knifeTools` display data),
`src/state/library.ts` + `src/components/ToolLibraryDialog.tsx` (knife tools
listed/edited as geometry, knife presets displayed),
`src/App.tsx`, `web/tests/sequence.test.tsx`, `tests/sim/sequenceSim.test.ts`
(+3).

Test evidence: `cargo test -p cam-core --locked --test knife_import` → 6
passed; `--test knife_stage` → 4 passed; `--test tool_library` → 14 passed;
full `cargo test --workspace --locked` 0 failures (53 test targets);
`cargo fmt --all -- --check` and clippy `-D warnings` pass; web 171 tests,
`typecheck`, `check:contracts`, `pnpm build` (wasm bindings) pass.

Remaining unsupported cases: the knife operation planner is F2 (alignment,
compensation, corner swivels, depth/closure, independent replay — enabled
knife operations still reject with `OPERATION_PLANNER_UNAVAILABLE`, and no
plan-producing code emits knife stages yet); the knife UI workflow and
trace/orientation playback arrive with F3 (the legacy library dialog lists
and edits knife tool geometry and displays knife presets, but editing knife
presets there is deferred; `apply_knife_to_job` is engine-level until F3
wires a UI command); chain simplification merges only sub-tolerance interior
vertices (smooth curves are linearized by the flattener as before, and
sub-10 µm fingerprints remain indistinguishable per D1); a knife-only job
cannot render the milling stock timeline yet (the heightfield needs one
milling tool — explicit error); the readback verifies spindle and coolant
state numerically but trusts the emitted per-stage path-control words only
through the prepared-state group assertions.

## F2: knife geometry — ✅ done (2026-09-10)

Boundary: `operations/drag_knife/{mod.rs,replay.rs}` — the passive-knife
planner (plan section 12): known-heading alignment from the explicit setting
(never a silent default), tangent holder compensation `q = p + d·t` with
radius-`d` corner swivel arcs centered on the tip corner, corner threshold
splitting explicit swivels (rise/swivel/re-enter at the resolved contact
depth) from continuous compensated arcs at cut depth, depth passes ending
exactly at the bottom with per-pass contact re-alignment from the carried
heading, closure corner + optional overlap handled exactly once per loop,
through-allowance and cut-depth-capability gates, and the independent
no-slip kinematic replay (`θ̇ = -dot(q̇, n(θ))/d`, RK4 with adaptive halving)
as the tip-path error gate over the emitted holder polyline. Wire:
DragKnife admitted in `plan_job`, `stock_history` skips knife stages
(traces never enter the milling model), ui-8 capabilities advertise
`drag_knife` with `knifeReplay: true`, and knife missing fields project per
field.

Acceptance criteria (from plan section 18):

- [x] Square/curve/concave/disconnected/closure fixtures meet tip-error
      budgets. (`knife_geometry.rs`: the plan-section-19.1 right-angle
      corner — tip corner (15,15), incoming +X, outgoing +Y, offset 1 —
      yields holder endpoints (16,15)/(15,16) with the swivel arc centered
      on the tip and headings 180→270
      (`right_angle_corner_swivel_matches_the_plan_numbers`, which also
      derives every motion's tip from its pivot+heading and finds it on the
      drawn chain); a 24-chord semicircle of 7.5° turns keeps compensation
      on every small vertex with the tip on the arc and no explicit swivels
      (`many_small_curve_turns_keep_compensation_and_tip_accuracy`); a
      concave -90° corner swivels clockwise with per-chord heading steps of
      -7.5° (`concave_right_turn_swivels_clockwise_around_the_corner`); the
      closed square with 2 mm overlap cuts exactly perimeter+overlap once
      with four swivel actions per pass — the closing corner exactly once —
      ending 2 mm past the seam
      (`closed_contour_cuts_the_loop_once_with_a_single_closing_swivel`);
      every fixture passes `check_plan`, which includes the replay gate
      inside the planner.)
- [x] Impossible alignment and ambiguous reversal are explicit.
      (`KNIFE_ALIGNMENT_UNAVAILABLE` for a chain starting outside the stock
      (contact needs material) and for an initial heading opposing the first
      cut within 0.5° — no arbitrary swivel side
      (`alignment_outside_stock_and_ambiguous_headings_are_explicit`);
      `KNIFE_CORNER_AMBIGUOUS` for the ~180° reversal corner
      (`near_reversal_corners_are_rejected_not_guessed`); closure overlap at
      or above one loop is `KNIFE_CLOSURE_RANGE`, below-stock bottoms beyond
      the allowance are `KNIFE_THROUGH_ALLOWANCE`, deeper-than-capability
      cuts are `KNIFE_DEPTH_CAPABILITY`, and a missing initial heading is a
      located missing field, never a default.)
- [x] Extra fixtures: the second disconnected chain enters at its carried
      heading — lifted rapid retains it, the contact alignment pivots around
      the planted tip from the carried 180° to the first-cut 270°, no
      teleport to the tangent (`disconnected_chain_entry_uses_carried_heading_in_contact`);
      closed-chain passes need no second alignment (the closing swivel
      realigns the seam); depth passes end exactly at the bottom and shallow
      passes swivel at full pass depth (resolved `min(swivel_depth, pass
      depth)` visible in the motion Z values); anchor starts rotate closed
      seams, reverse open chains from an endpoint anchor, and reject
      mid-chain anchors on open chains (`KNIFE_START_RANGE`); knife stages
      contribute nothing to `stock_history`
      (`stock_history_ignores_knife_stages`); the planner's program exports
      spindle-off/coolant-off/exact-path through the independent numeric
      readback (`planner_output_exports_through_the_independent_readback`).
- [x] Independent replay (plan section 12.5): aligned straight cuts replay
      exactly; a misaligned entry is rejected (~1 mm tip deviation) while
      its heading demonstrably converges to trailing along the line; lifted
      rapids retain the heading (the contact equation is never applied in
      air); step-budget exhaustion is `BudgetExhausted` → generation status
      `inconclusive`, never success; a ten-times tighter planner tolerance
      still replays within budget with finer chords
      (`replay_*` tests and
      `replay_convergence_with_a_finer_planner_tolerance`). Swivel
      linearization spends a quarter of the motion tolerance; the replay
      budget is the full tolerance measured against the intended tip
      polyline, with heading comparison tolerance derived from
      `2·budget/blade_offset`.

Changed files: `crates/cam-core/src/operations/drag_knife/mod.rs` (new
planner), `crates/cam-core/src/operations/drag_knife/replay.rs` (new
independent replay), `crates/cam-core/src/operations/mod.rs` (dispatch),
`crates/cam-core/src/sequence.rs` (DragKnife admitted in `plan_job`;
`stock_history` skips knife stages), `crates/cam-core/src/svg/mod.rs`
(fmt-only fix of a pre-existing misformatted line),
`crates/cam-service/src/sequence.rs` (capabilities advertise `drag_knife` +
`knifeReplay: true`; knife missing-field projection), tests:
`crates/cam-core/tests/knife_geometry.rs` (new, 18), `knife_stage.rs`
(F1 admission test flipped: unconfigured knife plans incomplete, disabled is
excluded), `sequence_plan.rs` (knife unsupported-kind fixture becomes the
incomplete-not-silent-skip contract), `cam-service/tests/sequence.rs`
(capabilities + knife ui-8 round trip: Open → Contours → missing-field
projection → strict UpdateSettings of the heading → Plan complete with
knife stage and `bladeHeadingDeg` motion pages).

Test evidence: `cargo test -p cam-core --locked --test knife_geometry` → 18
passed; full `cargo test --workspace --locked` 0 failures (54 test targets,
421 tests); `cargo fmt --all -- --check` and clippy `-D warnings` pass; web
171 tests, `typecheck`, `check:contracts`, `pnpm build` (wasm bindings) pass.

Remaining unsupported cases: sacrificial alignment leads are not implemented
(only the explicit initial-heading convention establishes the cold-start
state; plan section 12.3); contact/swivel engagement is checked against the
original stock rectangle only — prior milling removal is not consulted, so a
knife entry into already-cleared material is not detected; near-reversal
turns above 170° are rejected rather than sided (first-release rule);
closure overlap applies to closed chains only and must stay below one loop;
the replay's heading check trusts emitted per-stage process words only
through the prepared-state assertions (F1); corner swivels are linearized
G1 polylines (native G2/G3 is a later writer change); the knife UI workflow,
trace/orientation playback and emitted-program replay regression arrive with
F3 (motion pages already carry `bladeHeadingDeg` and knife purposes through
ui-8); each knife operation re-establishes alignment from its own setting
(carried heading never crosses a tool change).

## F3: knife Rust integration — ✅ done (2026-09-11)

Authority: [plan section 22.10](2.5d-cam-plan.md#2210-f3-revised-rust-knife-integration-and-output-evidence).
Scope revised 2026-09-10: Rust-only backend integration; no React/egui work.

Boundary: **F3a** in `post/sequence.rs` — `PreparedExecution::decode_program`
reconstructs the supported modal state from the actual program bytes:
interpolation (G0/G1), units/plane/distance/feed mode (G20/G21, G17-G19,
G90/G91, G93/G94), cutter compensation (G40/G41/G42), length compensation
(G43 H, stable within a stage), path control with the G61/G61.1/G64
distinction (blend P/Q compared exactly against the prepared mode), work
frame (G54-G59.3), tool changes, dwell, spindle (M3/M4/M5) and coolant
(M7/M8/M9); every unsupported G/M word, inline comment, oversized line,
G92.1-less leftover or post-M2 block is a located rejection, never a skip.
Decoded motions carry start/end/feed/tool plus their source line;
post-generated positioning bridges are matched one-for-one against the
`bridge_blocks` derivation shared with the writer, and the first block
after a tool change may stay unpositioned because the M6 macro's internal
motion remains machine-owned. **F3b** in
`operations/drag_knife/{evidence.rs,replay.rs}` — the independent no-slip
replay runs on the decoded, rounded output coordinates transformed back
through the exact work-zero inverse, against intended tips reconstructed
only from the trusted plan's modeled pivot/heading pairs
(`tip = q + d·u(θ)`; never from decoded points, comments or client
headings). `export` gates the emitted bytes on it, raising output
precision up to nine places until the replay is within budget
(`KNIFE_REPLAY_BUDGET` stays inconclusive-and-blocking, never truncated).
`KnifeEvidenceReport` (`knife_evidence` schema 1) is the renderer-neutral
bounded projection: per-sample pivot, intended tip, replayed tip, modeled
and replayed headings with motion/stage/pass/purpose ownership, plus
aggregate status and maxima computed over every boundary — a display
sample budget truncates detail, never outcome — bound to the execution
fingerprint, machine offset, output precision and exact program SHA-256.
**F3c** in `operations/drag_knife/mod.rs` — `check_prefix_contact` runs
the required contact corridors (cut segments, explicit swivel plant
points at the resolved swivel depth, continuous small-turn chords with
the linearization margin) against the preceding stock prefix's milling
sweeps (floor depth, cutter radius, reserve); intersecting removal makes
the knife operation incomplete with a located `KNIFE_CONTACT_UNSUPPORTED`
naming chain and prior operation, and export stays blocked, while a
knife-only job on intact stock stays trivially supported and knife
traces still never count as removal. **F3d** in `cam-service/src/sequence.rs`
+ `cam-app/src/sequence_cli.rs` + `cam-wasm`: `AddOperationKind::DragKnife`
creation (never-defaulted initial heading and every feed reported as
missing machining settings), `ApplyKnifeTool` applying
`tool_library::apply_knife_to_job` to exactly one knife assignment
without touching siblings, and `KnifeEvidence` returning the bounded
report plus physical stock for knife-only plans (no milling tool
required anywhere); shared by HTTP service, the `cam sequence
add-operation --kind drag_knife` / `apply-knife-tool` / `knife-evidence`
CLI commands and the WASM worker envelope under `ui-8`, with
`knifeEmittedEvidence` and `knifeContactChecks` advertised in
capabilities.

Acceptance criteria (from plan section 22.10):

- [x] **F3a — actual-byte state:** knife-only export decodes with every
      motion under exact path + spindle off + the prepared G54 frame and
      its true source line, the knife stage re-establishing G61 under a
      G64-blending profile
      (`knife_output.rs::knife_only_export_decodes_and_replays_within_budget`,
      `decode_program_reports_source_lines_and_modal_mapping` — motion
      starts continue from the previous block and the first block after
      M6 is unpositioned, never fabricated).
- [x] Decoded bridges: a declared program start writes Z and XY bridges
      consumed one-for-one; deleting or duplicating one fails the
      comparison (`bridge_blocks_are_accounted_exactly` →
      `POST_SEQUENCE_MISMATCH`).
- [x] Mutation fixtures: removing G61, substituting G61.1 or G64 P,
      stripping all path-control words, injecting G20/G91/G41/G28.1/M30,
      substituting the governing work frame, inserting M3/M8 during knife
      cuts, motion after M2 and a missing M2 are each rejected with their
      specific codes; an altered swivel coordinate and a deleted swivel
      line fail through the numeric comparison even though every other
      byte matches (`mutation_fixtures_cannot_evade_output_checks`;
      `knife_stage.rs::readback_rejects_spindle_or_coolant_running_during_knife_motion`).
      A work-frame substitution is caught when it governs a motion; an
      intermediate selection superseded before any motion is semantically
      invisible and correctly stays accepted (documented in the test).
- [x] **F3b — emitted replay:** the export report's `knifeReplay` gate
      records `within` after the precision-increase mechanism ran
      (`emitted_replay_gates_the_export_report`); a 0.5 mm holder shift
      that keeps the motion count and polyline connected still fails the
      independent replay with max deviation above budget
      (`emitted_replay_detects_geometry_deviation_by_itself`).
- [x] Bounded evidence: the report binds to the exact program SHA-256 and
      execution fingerprint, every sample's replayed tip derives from its
      pivot and heading within 1e-9, planted swivel samples reconstruct
      the intended tip from the modeled pair, and a one-sample budget
      reports truncation with identical totals, maxima and status
      (`evidence_report_binds_program_hash_and_reports_bounded_traces`).
- [x] Heading convention preserved: `knife_tip(pivot (11,0), 180°, d=1) =
      (10,0)` and the swivel plant points
      (`knife_stage.rs::knife_motions_carry_blade_headings_and_never_mill`,
      which also fails `PLAN_KNIFE_HEADING` for missing/stray headings and
      `PLAN_EFFECT_ROLE_MISMATCH` for effect/role cross-contamination).
- [x] Rounding/transforms: stock-bottom + center-XY work zero exports
      with machine offset (20, 0, -3), decoded setup coordinates recover
      within the output quantum and the replay stays within budget
      (`stock_bottom_work_zero_and_nonzero_xy_transform_round_trip`);
      rounded tiny swivels are the chords the planner emits
      (`knife_stage.rs` plan fixture subdivides the quarter-arc swivel);
      closure, disconnected chains and recurring tools are covered by the
      F2 fixtures replaying through export plus the mixed-program decode
      below.
- [x] **F3c — prefix contact:** whole-stock face → knife against the
      `FaceResult` plane completes, exports, decodes and replays within
      budget with tool changes [1, 3] and the milling stage decoding as
      blending/spindle-on while the knife stage is exact-path/off; a deep
      partial face that clears the chain's corridor leaves the knife
      operation incomplete with a located `KNIFE_CONTACT_UNSUPPORTED`
      naming `face-1`, and export is blocked by the incomplete generation
      (`mixed_face_then_knife_passes_and_cleared_air_is_rejected`).
- [x] **F3d — Rust interfaces:** typed creation reports the
      never-defaulted heading and feeds as missing
      (`add_operation_edits_create_incomplete_drag_knife_operations`);
      targeted application copies preset + new geometry snapshot
      (blade offset 1.5) to the chosen assignment while the sibling's
      distinct 300/2 values survive, and unknown operation/tool are
      located errors without partial rewrite
      (`apply_knife_tool_targets_one_assignment_and_preserves_siblings`);
      a knife-only job with no milling tool anywhere gets stock,
      bound traces and a passing basic-checks summary through the service
      and the same status through the WASM worker envelope
      (`knife_evidence_reports_bound_traces_stock_and_program_hash`,
      `cam-wasm/src/tests.rs::knife_commands_round_trip_through_the_worker_envelope`).
- [x] Validation executed: `cargo fmt --all -- --check`, `cargo clippy
      --workspace --all-targets --locked -- -D warnings`, `cargo test
      --workspace --locked` (all crates including cam-server and cam-wasm)
      pass; `cargo build -p cam-wasm --target wasm32-unknown-unknown
      --locked` builds on the pinned 1.95.0 toolchain. Runtime smoke: the
      cam-app test drives the compiled `cam` binary end-to-end —
      `sequence export` emits T3 M6 + G61 with no M3 and a
      `knifeReplay: within` report, `sequence knife-evidence --samples 16`
      publishes `within` with `initialHeadingDeg` 90 and ≤16 samples,
      `sequence add-operation --kind drag_knife` grows the document with
      the alignment left unset
      (`sequence_cli.rs::knife_job_adds_exports_and_publishes_evidence_through_the_cli`).
      No web/UI runtime checks were run (no `web/` changes in this slice).

Remaining unsupported cases: knife contact over prior milling is proved
only by the conservative corridor test — any intersecting removal rejects
the operation even where deeper material might survive, and no per-point
material proof exists yet (the plan-sanctioned F3 fallback); the contact
gate consults milling sweeps only; one evidence report covers plans whose
knife stages share one blade offset (`KNIFE_EVIDENCE_SOURCE` asks for one
report per tool otherwise); `verify_program` on imported programs does
not by itself rerun the emitted replay — the export path gates on it;
sacrificial alignment leads, near-reversal side selection, physical
slit/part simulation and native G2/G3 output remain deferred as in F2.

Excluded from F3: React/egui forms, picking dialogs, timeline widgets, trace/orientation
rendering, cameras and visual-heightfield implementation. These remain UI-plan work.
Sacrificial leads, near-reversal-side selection, physical slit/part simulation and native
arc output retain their current deferred status. Retained tasks, collection support and
sequential-file export belong to the H slices, not F3 acceptance.

## H1–H6: UI-driven backend additions

Implementation boundaries and acceptance are specified in
[plan section 22.11](2.5d-cam-plan.md#2211-backend-delivery-order-for-the-new-ui), with
fixtures and starting modules in
[section 22.12](2.5d-cam-plan.md#2212-addendum-acceptance-scenarios-and-file-map).
The summary rows above are placeholders for implementation tracking. Add per-slice
changed files, behavior, executed checks and limitations as work lands; do not reuse
the older A1–F2 test evidence to mark these additions complete.

## H1: portable document and draft contracts — ✅ done (2026-09-11)

Authority: [plan sections 22.2, 22.3, 22.5 and 22.11](2.5d-cam-plan.md#2211-backend-delivery-order-for-the-new-ui).
Starting point: `30659dd` (F3 complete); the then-uncommitted UI-planning
documentation was committed separately by the user as `c0f58b0` during this
slice and is preserved untouched — this slice's changes contain no
documentation-only commits and build on `c0f58b0`.

Boundary: the schema-5 canonical collection document in `cam-core` — artwork
items with split interpretation/placement, qualified geometry references with
source revisions, copied provenance (tool `library_origin`, assignment
`applied_profile` baselines including unset values) and the portable
`AppliedMachineConfiguration` shape — plus the layered validation split
(structural integrity vs reference inspection vs scope-gated planning
readiness) and migration from the frozen schema-4 DTO (and chained legacy
schema-1/2/3). The ui-8 client explicitly refuses schema-5+ documents. No
service transport, planner consumption or UI work (H2–H5 own those).

Acceptance criteria (from plan section 22.11):

- [x] The v4 migration DTO is frozen: `project::CamJob` schema 4 is the
      declared migration input, unchanged in shape (only the module doc and
      `pub mod v5;` wiring were touched), and every pre-existing v4 suite
      still passes; the frozen parser refuses a schema-5 document outright
      (`structural_integrity_still_rejects_malformed_documents`).
- [x] All incompatible schema-5 fields ship together: `artwork: Vec<ArtworkItem>`
      with `ArtworkContent::Svg(SourceSnapshot)` (tagged enum, only `Svg`
      implemented), `SvgInterpretation` (mode/tolerance/ticks, no placement)
      joined with item `Placement` through `ArtworkItem::import_options()`
      when calling the current importer — no top-level `source`/`import`
      fallback authorities exist (strict parsing rejects them);
      `GeometryRef { artwork_item_id, kind: filled_component | closed_contour
      | centerline, local_geometry_id, source_revision }`,
      `SourceRevision` (content+interpretation digest over exact embedded
      bytes — placement and display name excluded — plus algorithm version),
      `ContourAnchorV5`; `JobToolV5.library_origin`,
      `MillingAssignmentV5`/`KnifeAssignmentV5.applied_profile` with typed
      `CuttingBaseline` complete applicable fields including unset values;
      `machine_configuration: Option<AppliedMachineConfiguration>` whose
      preparation fields may stay unset. Round-trip identity proven
      (`provenance_and_machine_configuration_round_trip`).
- [x] Document integrity is separated from reference readiness:
      `validate_structure` (the save/open gate) covers shapes, finite
      values, unique valid IDs and bounded content (per-item 32 MB source
      limit, 64 MB document limit); `inspect_references` returns located
      saveable issues for missing artwork/tools/geometry
      (`ARTWORK_REFERENCE`, `PROJECT_TOOL_REFERENCE`, `GEOMETRY_REFERENCE`),
      revision mismatches (`ARTWORK_REVISION_MISMATCH`), import failures
      (`ARTWORK_IMPORT_FAILED`), forward/deleted face references
      (`HEIGHT_REFERENCE_FORWARD`, `PROJECT_HEIGHT_REFERENCE`), anchors
      needing reattachment (`CONTOUR_ANCHOR_UNRESOLVED`) and dangling
      machine mapping rows (`MACHINE_MAPPING_REFERENCE`). Malformed
      documents stay rejected (duplicate IDs, bad fractions, oversized
      items, unknown fields).
- [x] Source-free and single-source jobs migrate preserving placements,
      selections, anchors and missing values (`migrate_v4` +
      `migrate_json`): one deterministic item (`artwork-1`) with exact
      bytes, original interpretation and placement; the old-ID → qualified
      reference map is built from the migrated snapshot (catalogue rebuilt
      through a temporary frozen-v4 import job, one ID-derivation
      authority) and rewritten through every operation selection and
      start/tab anchor; anchor fractions and stored fingerprints are
      preserved with mismatches left unresolved, never repaired; placed
      geometry, fingerprints and perimeters are identical between the v4
      catalogue and the item catalogue
      (`single_source_job_migrates_with_placements_selections_and_anchors`,
      `source_free_face_job_migrates_to_an_empty_collection`,
      `migrate_json_routes_every_supported_schema` incl. the real m3
      fixture chained legacy→4→5, `migration_fabricates_nothing`: no
      machine configuration is invented from `legacy_machine_profile`, no
      provenance is invented).
- [x] A typed dangling reference round-trips and blocks only the affected
      planning scope: deleting the artwork item keeps the document saveable
      (`to_json`/`from_json` identity) while `planning_readiness` blocks
      exactly the referencing operations, leaves the face operation ready,
      keeps a prefix ending before the break plannable, and ignores
      disabled/unrelated operations
      (`dangling_references_round_trip_and_block_only_the_affected_scope`,
      `unknown_local_ids_migrate_to_typed_dangling_references`,
      `forward_face_references_are_saveable_and_block_only_their_operation`).
- [x] Content changes invalidate references until reattached — including
      edits smaller than the 0.01 mm fingerprint grid — while renaming or
      moving an item never does; identical bytes/settings retain references
      and identical-byte items stay distinct with per-owner resolution
      (`source_revision_changes_invalidate_references_until_reattached`,
      `identical_byte_items_stay_distinct`).
- [x] ui-8 compatibility stays explicit: `cam-service` `open` refuses
      schema-5+ documents with `SEQUENCE_SCHEMA_UNSUPPORTED` instead of
      flattening them to schema 4 or routing them through legacy migration
      (`schema5_collection_documents_are_refused_not_flattened`).

Changed files: `crates/cam-core/src/project.rs` (freeze doc + `pub mod v5;`
wiring only), `crates/cam-core/src/project/v5/mod.rs` (new: schema-5 shapes,
structural integrity, strict from/to_json), `crates/cam-core/src/project/v5/references.rs`
(new: per-item catalogue through reassembled temporary imports, reference
inspection, `ReadinessScope`-gated planning readiness), `crates/cam-core/src/project/v5/migrate.rs`
(new: v4→v5 with selection/anchor rewriting; chained legacy JSON entry),
`crates/cam-core/tests/project_v5.rs` (new, 13 tests),
`crates/cam-service/src/sequence.rs` (explicit schema-5+ refusal),
`crates/cam-service/tests/sequence.rs` (+1 test).

Test evidence: `cargo test -p cam-core --locked --test project_v5` → 13
passed; `cargo test -p cam-service --locked --test sequence` → 17 passed;
full `cargo test --workspace --locked` → 449 tests, 0 failures (56 result
suites); `cargo fmt --all -- --check` and `cargo clippy --workspace
--all-targets --locked -- -D warnings` pass; `cargo build -p cam-wasm
--target wasm32-unknown-unknown --locked` builds on the pinned toolchain.
Web unchanged: 171 tests, `typecheck`, `check:contracts` (schema-4/ui-8
drift pins) pass.

Remaining unsupported cases: schema-5 documents are not yet planned,
exported or transported (`ui-9`/`operation_plan` schema 2 arrive with
H3–H5; the current planners, sequence planning and export still run on
schema-4 jobs only, and `planning_readiness` is the cam-core gate H3's
planner integration will consult); numeric required-field readiness stays
with the planners (single authority) until H3 resolves collection geometry
into them; per-item catalogues are an internal helper here (inspection
imports every item on each call, no caching or command surface — H2's
document-level resolver and artwork/assignment commands); `ArtworkContent`
supports only `Svg` (Sketch reserved, unsupported kinds rejected by strict
parsing); provenance and machine-configuration shapes exist but
apply/reset/reapply/resolver behavior is H4; schema-4 selections without
any source migrate as references to the deterministic missing item
(`artwork-1`) and inspect as deleted artwork; extraction of embedded jobs
from existing schema-4 plan artifacts into v5 documents is not implemented
(old plans remain inspectable and regenerable through ui-8 exactly as
before); `FilledComponent` references resolve through the first catalogue
contour carrying the component ID; identity separation (document receipt vs
machining identity) is deferred to H3/H5 as planned.

## H2: artwork catalogue and document commands — ✅ done (2026-09-11)

Authority: [plan sections 22.3, 22.4, 22.5 and 22.11](2.5d-cam-plan.md#2211-backend-delivery-order-for-the-new-ui).
Starting point: `b527f7a` (GUI1 complete). The worktree also carries the
user's uncommitted GUI1 documentation edits (`docs/flat-v-carve/*`,
`experiments/gui1`); this slice changed none of them.

Boundary: **the document-level artwork resolver** in `project/v5/artwork.rs`
— per-item import through the one existing SVG importer (`item_catalogue`
moved here from `references.rs`, so migration, inspection and commands share
one import authority), `resolve_artwork_item`/`inspect_artwork` building the
combined owner-qualified catalogue (entries carry the derived wire ID
`{item}:{kind}:{local}`, reversible through `parse_wire_id`; suggested
sides, fingerprints, roles and setup-space bounds), and `placed_bounds`
over explicitly listed items. **Typed pure document commands** in
`project/v5/commands.rs`: batch `add_artwork` (per-file rejection for
oversized/unimportable content, accepted additions commit together, no
existing selection expands), `rename/duplicate/reorder/replace/remove/place`
artwork, selection assignment
(`set_component_selection`/`set_contour_selection`/`set_chain_selection`
over `GeometryPick`s — the command binds the item's current source revision
and validates every pick against the resolver, so assignments cannot carry
stale revisions), `reattach_anchor` (Start/Tab(profile) targets, fraction
preserved or explicitly replaced, fingerprint/revision bound from the
catalogue, never nearest-contour repair), and fit stock as the
proposal/commit split `propose_fit_stock`/`apply_stock_rectangle`.
`CamJobV5::to_json` now enforces the aggregate 64 MB serialized-document
budget (per-item limits alone never bounded the collection). Commands
return the updated document, the post-command reference issues and stable
affected IDs; failing commands cannot partially rewrite the document. No
service transport, planner consumption or UI work (H3–H5 own those; ui-8
still refuses schema-5+ documents).

Acceptance criteria (from plan section 22.11):

- [x] Same-name/identical-byte sources do not collide.
      (`same_name_identical_byte_sources_stay_distinct`: two files with the
      same filename and identical bytes import as distinct items (`art`,
      `art-2`); every catalogue entry's wire ID is unique across items and
      reverses to its pick — including the colon-carrying `plate::0`
      component spelling — with no raw filename material embedded; one
      profile selects contours from both and both resolve; the collection
      stays distinct through duplication, artwork reorder, save/reopen,
      while operation order never follows artwork row order.)
- [x] Moving one source leaves the others fixed.
      (`moving_one_source_leaves_the_other_fixed`: placing the lettering
      item changes only its placed bounds; the untouched item's geometry is
      bit-identical; references and anchor fractions survive the move of
      either the selected or the selecting item, and the moved item's own
      references stay bound to its unchanged source revision.)
- [x] Replacement/deletion retains actionable references.
      (`replacement_with_changed_content_keeps_typed_dangling_references`:
      a sub-fingerprint-grid edit changes the revision, producing exactly
      one located `ARTWORK_REVISION_MISMATCH` per reference-bearing field
      and blocking exactly the selecting operations while the face stays
      ready and the broken draft round-trips;
      `identical_replacement_retains_references`;
      `failed_replacement_changes_nothing` — unimportable content is
      rejected with the old source untouched;
      `removal_retains_references_and_reassignment_repairs` — after
      deletion the dangling draft round-trips, and the explicit repair
      route (re-add + reassign/reattach through commands, no
      nearest-item guessing) makes the job fully ready again.)
- [x] Extra contracts: batch import reports per-file rejection and commits
      accepted additions as one unit
      (`batch_import_commits_accepted_additions_and_reports_per_file_rejection`,
      adding an item never expands an operation's selection); duplicate
      creates a new item and qualified identities while nothing selects it
      and renames never invalidate references
      (`rename_and_duplicate_do_not_disturb_machining_references`);
      assignment commands validate picks (unknown item/local ID/wrong
      kind, on-contour without traversal, wrong operation kind) and allow
      clearing a selection as an incomplete state
      (`assignment_commands_validate_picks`); anchor reattachment binds
      revision+fingerprint from the catalogue, keeps fractions, and
      nonexistent targets/ranges are located errors
      (`reattach_anchor_repairs_after_replacement`); fit stock proposes
      union placed bounds plus per-side margins without mutating the
      document, fits exactly the listed items, and applying changes only
      `setup.stock.xy` (`fit_stock_proposes_bounds_and_applies_explicitly`);
      per-item inputs within limits but an oversized aggregate fail with
      `PROJECT_RESOURCE_LIMIT` and the prior document preserved
      (`aggregate_document_limit_preserves_the_prior_document`); unknown
      command targets and broken reorders are located errors that change
      nothing (`unknown_targets_and_broken_reorders_are_located_errors`).

Changed files: `crates/cam-core/src/project/v5/artwork.rs` (new),
`crates/cam-core/src/project/v5/commands.rs` (new),
`crates/cam-core/src/project/v5/mod.rs` (module wiring, re-exports, the
`to_json` aggregate budget), `crates/cam-core/src/project/v5/references.rs`
(`resolve_items` now consumes the shared resolver; `item_catalogue` moved
out), `crates/cam-core/src/project/v5/migrate.rs` (import path only),
`crates/cam-core/tests/artwork_commands.rs` (new, 13 tests),
`crates/cam-core/tests/project_v5.rs` (`item_catalogue` import path only).

Test evidence: `cargo test -p cam-core --locked --test artwork_commands` →
13 passed; full `cargo test --workspace --locked` → 462 tests, 0 failures
(57 result suites); `cargo fmt --all -- --check` and `cargo clippy
--workspace --all-targets --locked -- -D warnings` pass; `cargo build -p
cam-wasm --target wasm32-unknown-unknown --locked` builds on the pinned
toolchain. Web suite unchanged (no `web/` changes in this slice).

Remaining unsupported cases: schema-5 documents are still not planned,
exported or transported — the commands are cam-core functions with no
ui-9/CLI/HTTP surface yet (the ui-8 client continues to refuse schema-5+
documents; H4 owns the typed transport); `inspect_artwork` imports every
item on each call and every command commit runs a full reference inspection
(command-rate cost only, caching is a later optimization); the combined
catalogue carries bounded derived data (bounds/fingerprints/roles), not
full vertex lists — resolved planner geometry and precision-grid
reconciliation across sources are H3; creating a brand-new anchor
(vs reattaching an existing one), schema-5 operation CRUD and tool/profile
commands are later slices (`reattach_anchor` deliberately repairs only);
fit-stock bounds derive from placed catalogue geometry only; the aggregate
budget covers serialized JSON documents (binary caches and motion pages
have their own H5/H6 accounting).

## H3: multi-source machining and identities — ✅ done (2026-09-11)

Authority: [plan sections 22.4, 22.5, 22.8 and 22.11](2.5d-cam-plan.md#2211-backend-delivery-order-for-the-new-ui).
Starting point: `2ba296d` (H2 complete). The worktree still carries the
user's uncommitted GUI1 documentation edits (`docs/flat-v-carve/*`,
`experiments/gui1`); this slice changed none of them.

Boundary: **cross-source resolution feeding the existing planners**
(`project/v5/resolve.rs`, new): reference resolution with the H2 error
codes, per-item import through the shared resolver, the Flat V-carve
component union (`resolve_vcarve_region` — equal-tolerance items share
the identical grid by construction and union directly; mixed tolerances
plan on the finest selected grid after each coarser item independently
satisfies motion ≥ arc(t)+snap(t) and verification ≥ 8·t under its own
import error, else it is named in `ARTWORK_PRECISION_INCOMPATIBLE`), an
assembled owner-qualified contour catalogue (wire-ID string mapping onto
the frozen schema-4 settings shapes, in-memory only), settings mappers
and the four-kind v5 operation dispatch. **Planner geometry seams**:
each catalogue entry carries its owning item's placement (`contours.rs`)
so profile/knife anchors resolve through the right transform; pocket and
V-carve planning gained resolved-region entry points
(`plan_endmill_with_region`, `plan_combined_with_region`) so the legacy
import path and the v5 union path run identical machining algorithms;
the per-operation planners take a `PlanContext` (setup/tolerances/tools)
instead of a whole job, with value-based height resolution
(`resolve_heights_values`/`resolve_top_values`) decoupled from the
document; Flat V-carve separates its region-based core from document
assembly, and the v5 bridge (`to_legacy_job_v5`) carries an inert source
snapshot — the resolved region is the geometry authority, never a
re-import. **Sequencing and identity** (`sequence.rs`):
`OperationPlanV5` (artifact schema 2) with `plan_job_v5` over any
`ReadinessScope` — validation, readiness gating (blocked operations
become located Incomplete results while the rest of the job stays
inspectable), semantic prefix stock IDs, the machining identity (setup
stock/clearance/start XY, tolerances, referenced artwork sorted by item
ID with content revision+interpretation+placement, enabled operation
order with provenance-stripped settings, used tool geometry/capability
snapshots; excludes display names, provenance, artwork row order,
unreferenced items/tools and work zero) and the execution fingerprint
derived from it; `checks.rs` exposes the generic assembly check as
`check_plan_v5`.

Acceptance criteria (from plan section 22.11):

- [x] Old migrated outputs compare equal.
      (`migrated_job_plans_identically_through_both_models`: a schema-4
      job with profile/carve/knife operations plans through `plan_job`
      and through its migrated schema-5 document with identical motions
      (owner-qualified contour IDs normalized back to local IDs),
      stages, execution, per-operation results and a passing check;
      `real_combined_fixture_plans_identically_after_migration`: the
      real m4 fixture loads through the legacy loader, migrates and
      plans identically through both models with every operation
      Complete.)
- [x] Cross-source carve union.
      (`cross_source_carve_unions_selected_regions`: one carve selecting
      filled components from two items clears the plate interior to the
      full 1.5 mm depth, the word item's exclusive 8×6 mm strip to its
      depth-dependent first layer (a strip that narrow cannot admit the
      full-depth clearing layer of a 3 mm endmill), and the overlap once
      to the plate floor; material outside both regions stays untouched;
      the plate-only selection yields strictly fewer motions and never
      reaches the word's placed area;
      `face_referenced_cross_source_carve_admits_the_union_bounds`: a
      face-referenced carve over the union plans to the faced plane
      (deepest cut −2.0 mm), and shrinking the face so the union crosses
      its coverage is rejected with `SURFACE_REFERENCE_OUTSIDE_COVERAGE`.)
- [x] Mixed Profile/Knife selections span sources.
      (`mixed_profile_and_knife_selections_span_sources`: face → profile
      (outer contour from item 1, inner from item 2) → knife on item 1's
      placed chain; all Complete, the assembly check passes, the
      profile's rough motions hold both items' compensated loops (the
      plate's outer dilation reaches x < 0.5, the word's inside cut
      passes through its placed interior), the knife rides the placed
      chain at y = 5 with spindle-off preparation, and stage roles are
      Face/ProfileRough/Knife.)
- [x] Artwork row order/provenance does not alter machining; used-source
      edits do.
      (`machining_identity_separates_display_from_machining_changes`:
      renaming the job/an item/an operation/a tool, reordering artwork
      rows, adding unreferenced items/tools, library provenance and
      work-zero moves all keep identity and execution fingerprint
      current, while a used item's placement or content edit, an
      effective feed edit, reordering operations and disabling one each
      stale the identity;
      `prefix_dependencies_follow_used_source_edits_only`: a rename
      keeps every prefix stock ID in place while a used-source content
      edit stales the referencing operation's prefix and every later
      one.)
- [x] Readiness blocks only the referencing operations.
      (`stale_revision_blocks_only_the_referencing_operations`: after a
      content replacement the face stays Complete, the profile is
      Incomplete with a located `ARTWORK_REVISION_MISMATCH`, the
      sequence check fails closed for export, and the prefix ending
      before the break still plans and passes;
      `precision_gate_rejects_incompatible_coarser_items`: a 0.5 mm
      partner in the selection is named and blocked while the fine-only
      selection still plans.)

Changed files: `crates/cam-core/src/project/v5/resolve.rs` (new),
`crates/cam-core/src/project/v5/mod.rs` (module wiring),
`crates/cam-core/src/contours.rs` (per-entry placement),
`crates/cam-core/src/pocket/{mod.rs,settings.rs}` and
`crates/cam-core/src/vcarve/{mod.rs,settings.rs}` (resolved-region entry
points), `crates/cam-core/src/operations/mod.rs` (`PlanContext`,
`PlannerGeometry`), `crates/cam-core/src/operations/{profile/mod.rs,
face.rs, drag_knife/mod.rs, flat_vcarve.rs}` (context refactor plus the
v5 bridge), `crates/cam-core/src/setup.rs` (value-based height
resolvers), `crates/cam-core/src/sequence.rs` (`OperationPlanV5`,
identity, stock history), `crates/cam-core/src/checks.rs` (generic
assembly check), `crates/cam-core/tests/collection_machining.rs` (new,
9 tests).

Test evidence: `cargo test -p cam-core --locked --test
collection_machining` → 9 passed; full `cargo test --workspace --locked`
→ 471 tests, 0 failures (58 result suites); `cargo fmt --all
-- --check` and `cargo clippy --workspace --all-targets --locked -- -D
warnings` pass; `cargo build -p cam-wasm --target wasm32-unknown-unknown
--locked` builds on the pinned toolchain. Web suite unchanged (no `web/`
changes in this slice).

Remaining unsupported cases: no service transport or UI — schema-5
planning is a cam-core API only (the ui-8 client still refuses schema-5+
documents; H4 owns the typed transport and inspection DTOs); export
bundles over `OperationPlanV5` are deferred to H5 (the v5 path produces
plans, checks, preparation requirements and stock history, not scoped
output bundles); the machining identity is recomputed on demand over the
whole scope (no partial-invalidation bookkeeping between calls);
mixed-tolerance unions regrid coarser items onto the finest grid after
the budget gate (no cross-tolerance approximation sharing between
items); Flat V-carve
v5 still machines through the frozen legacy engine shapes (the wire-ID
mapping is in-memory only, so v5-only carve settings beyond the mapped
surface wait for later slices); knife replay beyond the F2 budget is
unchanged — a 1.5 mm blade offset on a short straight chain deviates
~0.5 mm in both schema-4 and schema-5 (pre-existing F2 limitation; the
fixtures use 1.0 mm like F2's own).

## H4: applied resources and service inspection — ✅ done (2026-09-11)

Authority: [plan sections 22.6, 22.7, 22.9 and 22.11](2.5d-cam-plan.md#2211-backend-delivery-order-for-the-new-ui).
Starting point: `a75568f` (H3 complete). The worktree still carries the
user's uncommitted GUI1 documentation edits (`docs/flat-v-carve/*`,
`experiments/gui1`, `.zcode/`); this slice changed none of them.

Boundary: **cutting-profile commands** (`project/v5/resources.rs`, new):
`AssignmentRole`-addressed apply/reset/reapply over exactly one assignment —
apply copies the preset's complete applicable field set including unset
values and stores the typed `CuttingBaseline`; reset restores the baseline
with no library input; reapply loads an explicitly supplied current revision
(removed records are located `RESOURCE_COMMAND` errors, never silent
no-ops); `apply_tool_to_assignment` copies an independent job-tool snapshot
with `LibraryOrigin` provenance, clears cutting fields and baseline only
when the tool id actually changes, and role-checks geometry kinds (knife
geometry never enters a milling assignment). **Applied machine
configuration** (`project/v5/machine.rs`, new): `apply_machine_configuration`
copies a validated schema-2 profile into the one editable snapshot — rows
for tools this job does not carry are dropped, never transplanted, and a
setup-clearance disagreement refuses with `MACHINE_CLEARANCE_CONFLICT`;
`set_tool_mapping` sets one row exactly (both numbers `None` removes it);
`resolve_sequence_profile` is the export gate:
`MACHINE_CONFIGURATION_ABSENT`/`_INCOMPLETE` (missing field list, nothing
fabricated), `MACHINE_MAPPING_MISSING`/`_INCOMPLETE` (per the selected
`ReadinessScope` — unused job tools require no T/H, tool-table compensation
requires H on active tools only) and `MACHINE_MAPPING_CONFLICT` (duplicate
active T), finishing with `validate_shape`. **Post-pipeline
generalization**: `SequencePlan`/`TrustedSequencePlan` traits over both plan
shapes (`TrustedPlanV5` joins `TrustedPlan`) so prepare/export/decode/
compare/knife-evidence run their identical algorithms over schema-5 plans;
`setup.rs` gained the values-variant work-zero resolver. **Inspection DTOs**
(`project/v5/inspection.rs`, new): used-by index (items, qualified
geometries, job tools→assignments), assignment statuses, flat machine
readout with per-row existence/usage annotations, and plan inspection over
`OperationPlanV5` — resolved heights against the faces the plan itself
published, face coverage, generated tab placements, ordered stage timeline
with motion spans, stock-prefix identities and stock extents (knife-only
jobs included, no milling tool invented). **ui-9 transport**
(`cam-service/src/collection.rs`, new): Open (migrate schema 1–4 once;
refuse >5 with `COLLECTION_SCHEMA_UNSUPPORTED`), Plan/Motions (replan-per-
page is the documented pre-H5 limitation), Inspect,
ApplyCuttingProfile/ResetAssignment/ReapplyProfile/ApplyTool,
ApplyMachineConfiguration/SetToolMapping/ResolveProfile, KnifeEvidence (the
applied machine configuration supplies the process profile — no separate
parameter) and Capabilities, with the ui-8 admission/envelope rules.
**Adapters**: `POST /api/v1/collection` (cam-server), `collection()`
export (cam-wasm) and `cam collection …` (cam-app CLI) share the projection.

Acceptance criteria (from plan section 22.11):

- [x] Cutting-profile baseline/reset/reapply with no live dependency
      (scenario 7). (`scenario7_two_named_profiles_on_one_tool_stay_independent`:
      two named presets land on job tool t1's two assignments (the partial
      preset copies its nulls), the sibling V-bit assignment stays Custom,
      editing one assignment marks only it Modified, reset without any
      library restores the copied baseline, and the reopened document
      describes itself with no library/configuration file;
      `reapply_uses_the_supplied_revision_and_fails_on_removed_records` and
      `tool_application_clears_only_on_tool_change_and_copies_provenance`
      pin the revision-change and tool-change semantics;
      `cutting_profile_commands_round_trip_through_the_transport` replays
      the same flow through ui-9.)
- [x] One applied machine configuration with active-scope mapping
      validation (scenario 8). (`scenario8_incomplete_snapshot_saves_and_mappings_follow_the_scope`:
      the incomplete snapshot saves and reopens without T numbers and
      resolves to `MACHINE_CONFIGURATION_INCOMPLETE` with the missing field
      list; application drops the foreign tool row; the prefix scope needs
      exactly its used tool, the full scope additionally names the unmapped
      knife tool (`MACHINE_MAPPING_MISSING`), mapping it completes the
      resolution, the machine readout equals the resolved profile's
      mappings, both-numbers-None removes the row and a duplicate active T
      blocks with `MACHINE_MAPPING_CONFLICT`;
      `machine_configuration_readouts_and_resolver_agree` replays it
      through the transport and `collection_route_…` through the HTTP
      route.)
- [x] Typed new transport and used-by/height/tab/knife inspection DTOs.
      (`inspection_reports_used_by_heights_and_stock`: used-by covers the
      item, the qualified chain and both t1 assignments; plan inspection
      resolves face-referenced heights against the plan's published plane
      (profile top −1.5, bottom −3.5), reports face coverage, stock extents
      and the initial stock identity, and the stage timeline's motion
      counts sum to the plan; `plan_and_knife_evidence_flow_through_the_transport`
      serves summary + inspection + a bounded motion page, and
      KnifeEvidence without an applied configuration is
      `MACHINE_CONFIGURATION_ABSENT`, with the applied one exporting and
      replaying `within`.)
- [x] Reopen with no library/configuration files; reset restores copied
      values; no mapping or preset authority is duplicated.
      (every scenario-7/8 test reopens from serialized JSON only; the
      baseline lives only in `AppliedProfile`, the mapping only in
      `AppliedMachineConfiguration`, and the resolver is the single
      schema-2 authority — the job stores no competing editable copy.)
- [x] Knife evidence completion over collection plans (F3-dependent).
      (`knife_evidence_replays_collection_output_within_budget`: a
      knife-only collection job resolves its profile from the applied
      snapshot, prepares and exports through `TrustedPlanV5` (G61 exact
      path in the bytes), decodes its own emitted output and replays it
      `within` budget bound to the exact program SHA-256 and execution
      fingerprint, with stock readouts present and no milling tool
      involved.)

Changed files: `crates/cam-core/src/project/v5/{resources,machine,inspection}.rs`
(new), `crates/cam-core/src/project/v5/{mod.rs,commands.rs}` (module wiring,
`pub(crate) commit`, `JobTool`/`MachineConfiguration` affected entities),
`crates/cam-core/src/{sequence.rs,post/sequence.rs,operations/drag_knife/evidence.rs,setup.rs}`
(SequencePlan trait generalization, shared scope/tools helpers),
`crates/cam-core/src/{contours.rs,project/v5/artwork.rs}` (serde on
`ContourRole`/`SetupBounds`, `Ord` on `GeometryRefKind`),
`crates/cam-service/src/{lib.rs,collection.rs}` (ui-9 transport, new),
`crates/cam-server/src/lib.rs` (`/api/v1/collection` route),
`crates/cam-wasm/src/{lib.rs,tests.rs}` (`collection` export + round trip),
`crates/cam-app/src/{main.rs,collection_cli.rs}` (`cam collection …`,
new), tests: `crates/cam-core/tests/collection_resources.rs` (new, 6),
`crates/cam-service/tests/collection.rs` (new, 6).

Test evidence: `cargo test -p cam-core --locked --test
collection_resources` → 6 passed; `cargo test -p cam-service --locked
--test collection` → 6 passed; full `cargo test --workspace --locked` →
485 tests, 0 failures (60 result suites); `cargo fmt --all -- --check` and
`cargo clippy --workspace --all-targets --locked -- -D warnings` pass;
`cargo build -p cam-wasm --target wasm32-unknown-unknown --locked` builds
on the pinned toolchain. Web suite unchanged (no `web/` changes in this
slice).

Remaining unsupported cases: motion pages still replan the submitted
document per request (retained plan handles and paging without replanning
are H5); the public export command and scope-bound output bundles/manifests
over `OperationPlanV5` are H5 (this slice's KnifeEvidence exercises the
single-program prepare/export path internally only); ui-9 has no
operation-list editing or settings-replacement commands yet (H2's document
commands cover artwork; operation CRUD for the new transport arrives with
the UI slices); undo grouping consumes `AffectedEntity` IDs but no undo
stack exists; the incumbent React UI is untouched and still a ui-7/ui-8
client (an explicit adapter or gating is H5/H6 work).
