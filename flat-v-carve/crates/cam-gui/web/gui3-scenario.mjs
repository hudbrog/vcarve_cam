import {createHash} from 'node:crypto';
import {readdirSync} from 'node:fs';
// GUI3's source-interaction tour. All edits use pointer/keyboard/file controls.
export async function gui3Scenario({control,edit,state,waitFor,send,evaluate,sleep,record,screenshot,readFileSync,click,pressKey,path,out}) {
  const source=readFileSync('fixtures/gui3/lettering.svg','utf8');
  await evaluate(`(()=>{const transfer=new DataTransfer();transfer.items.add(new File([${JSON.stringify(source)}],'lettering.svg',{type:'image/svg+xml'}));document.getElementById('cam').dispatchEvent(new DragEvent('drop',{dataTransfer:transfer,bubbles:true,cancelable:true}));})()`);
  await waitFor(s=>s.job?.name==='lettering.svg'&&!s.active,'lettering source');
  const screen=(s,[x,y])=>{
    const b=s.bounds,r=s.controls['Artwork viewport'],v=s.workspace.view,k=1.6/Math.max(b[2]-b[0],b[3]-b[1]);
    x=(x-(b[0]+b[2])/2)*k;y=(y-(b[1]+b[3])/2)*k;
    const u=x*Math.cos(v.yaw)-y*Math.sin(v.yaw),w=(x*Math.sin(v.yaw)+y*Math.cos(v.yaw))*(v.isometric?.65:1);
    return [(r[0]+r[2])/2+u*v.zoom*(r[3]-r[1])/2,(r[1]+r[3])/2-w*v.zoom*(r[3]-r[1])/2];
  };
  const pick=async(point,modifiers=0)=>{
    const p=screen(await state(),point);
    await send('Input.dispatchMouseEvent',{type:'mouseMoved',x:p[0],y:p[1]});
    for(const type of ['mousePressed','mouseReleased'])await send('Input.dispatchMouseEvent',{type,x:p[0],y:p[1],button:'left',clickCount:1,modifiers});
    await sleep(250);
  };
  const overlap=readFileSync('fixtures/gui3/overlap.svg','utf8');
  const drop=async(text,name)=>evaluate(`(()=>{const transfer=new DataTransfer();transfer.items.add(new File([${JSON.stringify(text)}],${JSON.stringify(name)},{type:'image/svg+xml'}));document.getElementById('cam').dispatchEvent(new DragEvent('drop',{dataTransfer:transfer,bubbles:true,cancelable:true}));})()`);
  await drop(overlap,'overlap.svg');await waitFor(s=>s.job.name==='overlap.svg'&&!s.active,'overlap source');
  await pick([15,16]);const first=await state();await control('Next overlap');const second=await state();
  if(first.picked[0].local_geometry_id===second.picked[0].local_geometry_id || first.revision!==second.revision || second.job.components!==0)throw new Error('Overlap cycle lost distinct owners or changed machining');
  record('coincident filled candidates cycle without machining changes',second.picked);
  await drop(source,'lettering.svg');await waitFor(s=>s.job.name==='lettering.svg'&&!s.active,'lettering restored for tour');
  let before=await state();await pick([4,20]);let picked=await state();
  if(picked.picked.length!==1 || picked.job.components!==0 || picked.revision!==before.revision)throw new Error('Ordinary artwork pick changed machining or missed the L');
  await pick([26,15]);if((await state()).picked.length!==0)throw new Error('Hole was selected as filled artwork');
  await pick([4,20]);await pick([19,20],8);if((await state()).picked.length!==2)throw new Error('Shift multi-selection failed');
  await control('Use picked');await waitFor(s=>s.job?.components===2&&!s.active,'explicit use assignment');
  await pick([19,20]);await control('Remove picked');await waitFor(s=>s.job?.components===1&&!s.active,'remove picked assignment');
  await control('Add picked');await waitFor(s=>s.job?.components===2&&!s.active,'add picked assignment');
  record('qualified picking holes multi-select and explicit assignment',await state());
  const drag=async(mode,start,end,cancel=false)=>{
    await control(mode);await sleep(150);const s=await state(),a=screen(s,start),b=screen(s,end);
    await send('Input.dispatchMouseEvent',{type:'mouseMoved',x:a[0],y:a[1]});
    await send('Input.dispatchMouseEvent',{type:'mousePressed',x:a[0],y:a[1],button:'left',buttons:1,clickCount:1});
    for(let i=1;i<=8;i++){await send('Input.dispatchMouseEvent',{type:'mouseMoved',x:a[0]+(b[0]-a[0])*i/8,y:a[1]+(b[1]-a[1])*i/8,buttons:1});await sleep(45);}
    if(cancel===true)await pressKey('Escape','Escape');
    if(cancel==='undo')await pressKey('z','KeyZ',2);
    await send('Input.dispatchMouseEvent',{type:'mouseReleased',x:b[0],y:b[1],button:'left',buttons:0,clickCount:1});await sleep(700);
    return s;
  };
  before=await drag('Move artwork',[4,20],[9,23]);
  let moved=await waitFor(s=>!s.active&&s.revision>before.revision,'move release');
  if(Math.abs(moved.job.placement.origin_mm.x+5)>.05 || Math.abs(moved.job.placement.origin_mm.y+3)>.05 || moved.undo!==before.undo+1)throw new Error('Move used a wrong conversion or multiple transactions');
  await control('Undo');await waitFor(s=>!s.active&&s.job.placement.origin_mm.x===0,'gesture undo');
  before=await drag('Move artwork',[4,20],[7,21],true);let cancelled=await state();
  if(cancelled.revision!==before.revision || cancelled.job.placement.origin_mm.x!==0)throw new Error('Escape committed a placement gesture');
  before=await drag('Move artwork',[4,20],[7,21],'undo');
  const interrupted=await waitFor(s=>!s.active,'edit during gesture settles');
  if(interrupted.job.placement.origin_mm.x!==0 || interrupted.undo!==before.undo-1)throw new Error('Gesture committed across a document revision change');
  await control('Redo');await waitFor(s=>!s.active&&s.job.components===2,'restore assignment after stale-gesture test');
  const angle=(Math.atan2(20,8)-Math.atan2(8,20))*180/Math.PI;
  await drag('Rotate artwork',[20,8],[8,20]);await waitFor(s=>!s.active&&Math.abs(s.job.placement.rotation_deg-angle)<.7,'rotation gesture');
  await control('Undo');await waitFor(s=>!s.active&&s.job.placement.rotation_deg===0,'rotation undo');
  await drag('Scale artwork',[10,10],[15,15]);await waitFor(s=>!s.active&&Math.abs(s.job.placement.scale-1.5)<.02,'uniform scale gesture');
  await control('Undo');await waitFor(s=>!s.active&&s.job.placement.scale===1,'scale undo');
  await edit('Origin X','-5');await edit('Origin Y','-3');await waitFor(s=>!s.active,'numeric placement preview');
  record('pointer move rotate scale cancel and numeric equivalence',await state());
  await screenshot('gui3-lettering-placement.png');
  await control('Artwork');await control('Select artwork');await pick([9,23]);await control('Use picked');
  await waitFor(s=>s.job.components===1&&!s.active,'lettering subset assignment');
  await control('Generate');let issues=await waitFor(s=>s.issues?.length>10&&!s.active,'typed missing machining issues');
  const stockIssue=issues.issues.findIndex(i=>i.field_path==='setup.stock.thickness_mm');
  await control('Issue '+stockIssue);await sleep(350);
  const located=await state(),field=located.controls['Stock thickness'],clip=located.controls['Inspector viewport'];
  if(located.workspace.inspector!==1 || !field || field[1]<clip[1] || field[3]>clip[3])throw new Error('Issue navigation did not reveal its exact stock field');
  await pressKey('a','KeyA',2);await send('Input.insertText',{text:'10'});
  await waitFor(s=>s.job.stock.thickness_mm===10,'issue focuses stock thickness input');
  record('typed issue reveals and focuses owning field',await state());
  for(const [label,value] of [['Stock minimum X','-10'],['Stock minimum Y','-10'],['Stock width','65'],['Stock length','50'],['Clearance','5'],['Start X','0'],['Start Y','0'],['Motion tolerance','0.01'],['Verification tolerance','0.05']])await edit(label,value);
  await control('Endmill tool');await edit('Endmill diameter','2.5');await edit('Cutting length','15');
  await control('V-bit tool');for(const [label,value] of [['V-bit angle','90'],['Tip diameter','0.1'],['Cutting diameter','12'],['Cutting height','5']])await edit(label,value);
  await control('Cutting');await control('Depth-dependent clearing');
  for(const [label,value] of [['Maximum depth','1'],['Wall allowance','0'],['Floor ridge','0.1'],['Roughing feed','1200'],['Plunge feed','400'],['Stepdown','0.5'],['Stepover','1'],['Spindle speed','12000'],['Rough layer limit','30'],['Ramp angle','5'],['Ramp feed','250']])await edit(label,value);
  await control('Ramp yes');await control('Ramp entry');await control('Deepest-region clearing');await control('Depth-dependent clearing');
  // Restore plunge for the shared fixture; the ramp draft must remain available.
  await control('Plunge entry');await control('Endmill CW');await control('Plunge yes');
  await control('Combined');
  for(const [label,value] of [['Finishing feed','1000'],['Finish plunge','300'],['Finish stepdown','1'],['Finish stepover','1'],['Finish spindle','12000'],['Detail residual','0.1'],['Finish path limit','60000'],['Quality sample spacing','0.5']])await edit(label,value);
  await control('V-bit CW');await control('V-bit plunge yes');
  await control('Machine');await control('Example machine');await control('Apply flower machine profile');await waitFor(s=>s.job.machine&&!s.active,'lettering machine');
  await edit('Tool number','3');await edit('V-bit tool number','8');
  await waitFor(s=>!s.active&&!s.pending,'lettering settings complete');
  await control('Generate');await waitFor(s=>s.current&&!s.active&&s.motions>0,'lettering combined generation',120);
  await control('Simulate');await control('Top');await control('After V-bit');await waitFor(s=>!s.active&&s.stockPrefix===s.motions,'lettering final stock');
  await pick([9,23]);await control('Inspect result');await control('Pin comparison');await waitFor(s=>s.inspection?.pinned,'pinned final-stage snapshot');
  const original=await state();await screenshot('gui3-lettering-inspection.png');
  await control('Cutting');await edit('Detail residual','0.05');await waitFor(s=>!s.current&&!s.active,'detail edit marks result stale');
  await screenshot('gui3-lettering-stale.png');await control('Generate');
  await waitFor(s=>s.current&&!s.active&&s.inspection.comparisonPrefix===s.stockPrefix,'regenerate matching pinned stage',120);
  await control('Inspect result');
  record('detail edit stale regenerate and equivalent stage comparison',{before:original.inspection,after:(await state()).inspection});
  await screenshot('gui3-lettering-comparison.png');
  await control('Prepare checked output');await waitFor(s=>s.prepared&&!s.active,'lettering checked output',120);
  await evaluate('globalThis.showSaveFilePicker=undefined');await control('Save checked bytes');await waitFor(s=>s.status.includes('Download requested'),'lettering export download');
  const prepared=(await state()).preparedSha256;
  await sleep(1000);const outputs=readdirSync(out).filter(name=>/\.(nc|ngc|gcode|tap)$/.test(name));
  if(outputs.length!==1 || createHash('sha256').update(readFileSync(path.join(out,outputs[0]))).digest('hex')!==prepared)throw new Error('Downloaded lettering output differs from checked bytes');
  record('lettering subset generated simulated and exported',prepared);
  await control('Prepare');await control('Cutting');await control('Endmill only');await waitFor(s=>!s.active&&!s.pending,'endmill-only preview settled');
  await control('Save job');await waitFor(s=>s.status.includes('Download requested'),'lettering job saved');await sleep(700);
  const saved=readFileSync(path.join(out,'carving.gui2.job.json'),'utf8'),portable=JSON.parse(saved),settings=portable.operations[0].settings.settings;
  if(settings.finish.max_paths!==60000 || settings.finish.quality_sample_spacing_mm!==.5 || settings.rough.max_layers!==30 || settings.vbit.cutting_feed_mm_min!==1000)throw new Error('Portable inactive settings lost');
  const reopenRevision=(await state()).revision;
  await evaluate(`(()=>{const transfer=new DataTransfer();transfer.items.add(new File([${JSON.stringify(saved)}],'lettering-saved.job.json',{type:'application/json'}));document.getElementById('cam').dispatchEvent(new DragEvent('drop',{dataTransfer:transfer,bubbles:true,cancelable:true}));})()`);
  await waitFor(s=>s.revision>reopenRevision&&s.job.mode==='endmill_only'&&!s.current&&!s.active,'lettering reopen');await control('Combined');await waitFor(s=>!s.active&&!s.pending,'reopened combined preview settled');await control('Generate');await waitFor(s=>s.current&&!s.active,'reopened combined generation',120);
  record('portable inactive finish settings reopen and generate',await state());
}
